# remote_ci_monitor — 계획서 (v0, 2026-09-04)

> 이 문서는 첫 세션의 출발점이다. 맨 아래 「세션 시작 프롬프트」를 그대로 붙여 넣으면 된다.
> 결정이 필요한 항목은 ⛔ 로 표시했고 각각 기본값을 적어 두었다. 기본값이 마음에 들면 그냥 진행한다.

## 한 줄

**self-hosted GitHub Actions 러너에서 지금 뭐가 돌고, 누가 시켰고, 어느 스텝을 몇 분째 도는지, 그 머신의 CPU·메모리는 어떤지** 를 어느 컴퓨터에서든 웹 화면 하나로 본다.

## 왜 만드나

러너가 한 대뿐인 팀은 CI 요청이 여러 사람·여러 세션에서 겹친다. GitHub Actions 화면은 run 목록만 보여주고 「내 차례가 언제 오는지」「앞 run 이 걸린 건지 느린 건지」「러너 머신이 지금 버거운지」를 안 알려준다.
이 문제를 한 레포 안에서 터미널 스크립트로 한 번 풀어 봤다(private 레포 `fmmc-tech/dolomood-app-renew` 의 `scripts/ci_top.py`·`ci_queue.py`). 잘 돌지만 그 레포·그 머신·그 팀 규약에 묶여 있다. 이 프로젝트는 그걸 **누구나 자기 러너에 붙일 수 있는 독립 도구**로 다시 만든다.

## 반드시 지킬 것 — 이식성

이 레포는 public 이고 다른 사람이 자기 환경에서 쓴다. 아래는 위반하면 안 된다.

- 특정 머신·계정·팀 규약을 코드에 박지 않는다. 금지 예: 러너 이름 `dolomood-macmini`, gh 계정 `pcs-fmmc`, 시간대 KST 고정, `~/actions-runner` 존재로 「러너 머신인지」판별, run 이름 형식 `ci · <scope> · <branch> · ← <caller>`.
- 전부 설정으로 받는다(파일 + 환경변수 + CLI 플래그). 위 팀 규약은 `examples/` 의 예시 설정 한 벌로만 남긴다.
- run 이름 규약이 없어도 동작해야 한다. 요청자는 기본적으로 GitHub API 의 `triggering_actor` 로, 범위(scope)는 워크플로 이름으로 잡고, 규약이 있는 팀은 정규식(named group `scope`·`caller`)을 설정으로 더한다.
- macOS 러너만 가정하지 않는다. 호스트 자원 수집기는 macOS(`vm_stat`/`top`/`ps`)와 Linux(`/proc/meminfo`·`/proc/stat`·`/proc/loadavg`)를 둘 다 지원한다. Windows 는 범위 밖으로 명시한다.
- 설치가 한 줄이어야 한다. `pipx install` 또는 `uvx`, 그리고 Docker 이미지. 로컬 클론 없이 돌 수 있어야 한다.
- README 는 영어로 쓴다(사용자층이 한국어 팀 밖으로 열린다). 이 계획서 같은 내부 문서는 한국어여도 된다.
- 시크릿(GitHub 토큰, 수집기 공유 토큰)은 환경변수나 설정 파일로만 받고 절대 커밋하지 않는다. `.gitignore` 와 gitleaks 류 검사를 CI 에 건다.

## 무엇을 보여주나

| 항목 | 출처 | 비고 |
|---|---|---|
| 러너 상태 (online/offline · busy/idle) | `GET /repos/{o}/{r}/actions/runners` | 러너 여러 대 지원 |
| 큐 (FIFO) — 실행중/대기 · 범위 · 브랜치 · 요청자 · 잔여 · 예상 완료 | `GET .../actions/runs` | 예상 완료 = 최근 N 일 잡 실행시간 중앙값 − 경과, 앞선 run 잔여 누적. 레인 수(동시 실행 가능 잡 수) 설정 |
| 진행 — 스텝 N/M · 지금 스텝 · 그 스텝 경과 · 잡 전체 경과 · 스텝 타임라인 | `GET .../actions/runs/{id}/jobs` | 아래 「jobs API 실측 함정」 필수 |
| 최근 완료 — 결과 · 실측 소요 | runs + jobs | |
| 호스트 자원 — load · CPU % · 메모리 · 상위 프로세스 | 러너 머신의 수집기가 push | 원격에서 보이려면 어딘가로 내보내야 한다(⛔ 전송 방식) |

## 구조 (제안)

```
[러너 머신]                          [아무 데나]                    [브라우저]
collector ──push(JSON, N초)──▶  server ──/api/status──▶  web UI (정적 HTML+JS, 자동 갱신)
(vm_stat/top/ps 또는 /proc)     (GitHub API 폴링 + 캐시)
```

- `server` 하나가 GitHub API 를 폴링해 JSON 을 만들고 정적 UI 를 서빙한다. 어디서 돌려도 된다(러너 머신 자체, 노트북, 작은 VPS).
- `collector` 는 러너 머신에서만 돌고 호스트 자원을 server 로 push 한다. 러너 머신에 인바운드 포트를 열지 않아도 되게 **push 방향**을 기본으로 한다.
- 순수 계산(큐·잔여·진행률·파서)과 I/O(GitHub API·프로세스 실행·HTTP)를 모듈로 가른다. 순수 부분은 픽스처로 테스트한다.
- `--json`/`/api/status` 스키마를 먼저 고정하고 UI 는 그걸 소비한다. 터미널 한 컷 출력(`rcm top`)도 같은 스키마에서 그린다.

## jobs API 실측 함정 (2026-09-04, 픽스처로 잠글 것)

- 아직 안 시작한 스텝은 `status: "pending"` 이다(`queued` 가 아니다).
- post 스텝(`Post Run actions/checkout@v4`, number 14)은 잡이 도는 중에 이미 보이지만 `Complete job`(15)은 끝날 때 붙는다. 그래서 **알려진 스텝 수가 도중에 는다**(8 → 9). 「N/M」의 M 은 「지금까지 알려진 개수」라고 UI 에 밝혀야 한다.
- 스텝 `number` 는 연속이 아니다(1~7 다음 14, 15). 「N번째」는 목록 위치로 센다.
- 러너가 아직 안 집어간 잡은 `steps: []` 이고 `runner_name: null` 이다. 이때 `started_at` 이 있어도 시작이 아니라 `created_at` 과 같다. 대기 시간은 `created_at` 으로 잰다.
- run 의 `run_started_at` 은 큐 진입 시각이라 대기시간이 섞인다. 실행시간 표본은 **잡의** `started_at`~`completed_at` 으로만 만든다.
- 예상보다 오래 끄는 run 의 잔여를 음수로 두면 큐 전체가 앞당겨진다. 최소값(예 30초)으로 막되, 화면에는 실제 경과(「14분째」)를 그대로 보여 「걸렸다」를 사람이 알 수 있게 한다.

## fail-open 금지 (이 도구의 핵심 규칙)

아무도 안 쳐다보는 보조 화면일수록 조용히 고장나면 **틀린 값을 자신있게 보여주게 된다.**

- 조회에 실패한 칸은 「조회 실패」로 그린다. 실패를 0건·0/0·0초로 그리지 않는다.
- API 의 「실패」와 「빈 목록」을 코드에서 다른 값으로 돌려준다(예: `None` vs `[]`).
- JSON 스키마도 실패를 `null` + `*_error` 로 가른다. 수집기가 안 오면 「마지막 수신 N분 전」을 같이 찍는다(stale 표시).
- 순수 계산 모듈은 자기검증 테스트를 갖고 CI 가 매번 돌린다. 테스트가 실제로 빨개지는지 뮤테이션으로 한 번 확인한다(값 변이·동치 리팩터·검사 대상 삭제 중 최소 하나).

## 마일스톤

- M0 — 뼈대: 패키지 구조, 설정 로딩(파일·env·플래그), GitHub API 클라이언트(토큰은 `GITHUB_TOKEN` → `gh auth token` 순), 큐·잔여·진행률 순수 계산 + 픽스처 테스트, `rcm top` 터미널 출력, `rcm serve` 의 `/api/status`. CI(GitHub-hosted, public 이라 무료)에 테스트·lint·시크릿 스캔.
- M1 — 웹 UI: 러너 카드, 큐 표(진행 바·스텝 타임라인), 최근 완료, 자동 갱신, 모바일에서 읽히는 레이아웃, 다크/라이트. 빌드 도구 없이 정적 파일로.
- M2 — 수집기: `rcm collect --push <server>`(macOS·Linux), 공유 토큰 인증, stale 표시. 대안 전송(⛔) 하나 더.
- M3 — 배포: `pipx`/`uvx`, Docker 이미지, `examples/`(일반 · run 이름 규약 팀), README(영어).
- M4 — 확장: 레포 여러 개·러너 여러 대, run 취소 같은 쓰기 동작(선택), 알림(선택).

## ⛔ 결정 항목 (기본값 병기 — 기본값이면 질문 없이 진행)

1. **언어·런타임** — 기본 Python 3.11+. 근거: 참고 구현이 Python 이고 수집기가 러너 머신에 가볍게 깔려야 한다. 웹 프레임워크는 표준 라이브러리 `http.server` 로 시작해 필요해지면 FastAPI 로.
2. **호스트 자원 전송** — 기본 「수집기 → server HTTP push」. 대안: GitHub gist 를 주기 갱신(서버 없이도 동작, 지연 큼). 러너 머신에 인바운드 포트를 여는 방식은 기본으로 삼지 않는다.
3. **시간대** — 기본은 서버 로컬 시간대, 설정으로 IANA 이름을 받는다. 예시 설정에서 `Asia/Seoul`.
4. **레포 범위** — 기본 레포 하나. 설정에 여러 개를 나열하면 한 러너를 공유하는 레포들의 큐를 합쳐 본다(러너 이름으로 묶는다).
5. **UI 스택** — 기본 정적 HTML + vanilla JS(빌드 없음). 화면이 커지면 그때 바꾼다.

## 참고 구현 (private — 접근 가능한 사람만)

`fmmc-tech/dolomood-app-renew` 의 `scripts/ci_queue.py`(큐·중앙값·잔여 계산, 21개 자기검증) 와 `scripts/ci_top.py`(진행률·파서·렌더, 18개 자기검증), 문서 `docs/renew-guide/ci-cd/30-remote-dispatch.md`. 설계와 함정은 가져오되 코드는 이식성 규칙에 맞게 다시 쓴다. 픽스처를 옮길 땐 브랜치명·요청자 라벨·run id 같은 팀 정보는 지운다.

---

## 세션 시작 프롬프트 (복사해서 붙여 넣기)

```
이 레포(remote_ci_monitor)는 self-hosted GitHub Actions 러너의 큐·요청자·스텝 진행·호스트 자원을
어느 컴퓨터에서든 웹 화면으로 보는 독립 도구다. PLAN.md 가 정본이다 — 먼저 끝까지 읽어라.

이번 세션 목표: M0 를 끝낸다.
  1. 패키지 뼈대 + 설정 로딩(설정 파일 · 환경변수 · CLI 플래그, 우선순위 명시)
  2. GitHub API 클라이언트 — 토큰은 GITHUB_TOKEN → `gh auth token` 순, 없으면 명확한 에러
  3. 순수 계산 모듈(큐 FIFO · 잡 실행시간 중앙값 · 잔여 · 스텝 진행률 · 호스트 자원 파서) + 픽스처 테스트.
     PLAN.md 의 「jobs API 실측 함정」 여섯 개를 각각 테스트 하나로 잠근다.
  4. `rcm top`(터미널 한 컷)과 `rcm serve`(`/api/status` JSON). 둘 다 같은 스키마에서 그린다.
  5. CI: 테스트 · ruff · 시크릿 스캔. public 레포라 GitHub-hosted 러너를 써도 된다.

지킬 것:
  - PLAN.md 「반드시 지킬 것 — 이식성」을 위반하는 코드는 쓰지 마라. 특정 머신·계정·팀 규약을
    코드에 박지 말고 전부 설정으로 받아라. run 이름 규약이 없어도 동작해야 한다.
  - 「fail-open 금지」 — 조회 실패를 0건·0/0·0초로 그리지 마라. 실패와 빈 값을 다른 타입으로 돌려줘라.
  - 순수 계산과 I/O 를 모듈로 갈라라. 순수 모듈은 픽스처 테스트가 있어야 하고, 테스트가 실제로
    빨개지는지 뮤테이션 하나로 확인한 뒤 「검증됨」이라고 말해라.
  - ⛔ 결정 항목은 PLAN.md 의 기본값으로 진행하고, 기본값에서 벗어나야 할 이유가 생기면 그때 물어라.
  - README 는 영어, 커밋 메시지는 Conventional Commits.

끝나면: 무엇을 만들었는지, 테스트가 몇 개이고 어떤 뮤테이션으로 확인했는지, M1 에서 먼저 정해야 할 것이
무엇인지 짧게 보고해라.
```
