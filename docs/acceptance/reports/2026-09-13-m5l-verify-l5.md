# M5l 검증 보고 — L5 `serve` 포트 선점 · `sqlite3.Error`/`OSError` 한 줄 (`fix/serve-bind-before-migrate` · PR #118)

- 날짜: 2026-09-13
- 검증한 커밋: `origin/fix/serve-bind-before-migrate` **2a8cf13** (`fix(server): serve 는 포트를 잡은 뒤에 DB 를 연다 — sqlite 오류도 한 줄 거절`)
- 명세: `docs/acceptance/m5l-verification-scenarios.md` §0 · §1 L5 (L5.1 ~ L5.11) · 근거 리뷰 `docs/reviews/2026-09-13-impl-reviews/pr-94.md` B-1 ~ B-3 (B-4·B-5 기록)
- 워크트리: `remote_ci_monitor-verify-m5l-l5` (`docs/acceptance-m5l-verify-l5`, 기준 2a8cf13) · 자기 `.venv` (Python 3.11) · `DB_VERSION` = **16** (아래 `V`)
- 시험 서버: **8814** 정상 서버 — `<HOME>/.local/share/rcm-verify-m5l-l5.toml` (`bind 127.0.0.1` · `advertise = false` · `[notify]` 없음 · 프리셋 `ok` · `data_dir …/rcm-verify-m5l-l5`), `nohup ./.venv/bin/rcm serve --config … &`. **8815** 는 점유용 `python3 -m http.server 8815 --bind 127.0.0.1`. 옛 버전 픽스처 `…/rcm-verify-m5l-l5-old`(설정 `…-l5-8815.toml`, `port = 8815`): 현재 빌드로 한 번 열고 `PRAGMA wal_checkpoint(TRUNCATE); PRAGMA user_version=15` 뒤 `-wal`·`-shm` 삭제. 행마다 `data_dir` 을 따로 두었다(`…-garbage` · `…-ro` · `…-file` · `…-lock` · `…-nopre`). 끝나고 서버 둘을 내리고 `data_dir`·설정을 전부 지웠다.
- 주의(측정 순서): `sqlite3 <db> "PRAGMA user_version"` 을 치는 것 자체가 WAL 모드 DB 에 `-wal`·`-shm` 을 만든다. 「`-wal` 없음」은 **`ls` 를 PRAGMA 읽기보다 먼저** 해서 봤다(처음 한 번 순서를 거꾸로 해 헛것을 봤고, 다시 쟀다).

## 판정표

| ID | 판정 | 실측 | 명령/비고 |
|---|---|---|---|
| L5.1 | **통과** | `rc=2` · stderr **1줄** `rcm: cannot start server: Address already in use` · serve 직후 `ls <data_dir>` = `rcm.sqlite3`(`-wal` 없음 · `-shm` 없음 · `backup/` 없음) · 그 뒤 `PRAGMA user_version` **15**. 같은 실행을 프로세스 안에서 `sqlite3.connect` 를 감시하며 돌리니 **connect 호출 0회** — DB 를 연 적이 없다 | 8815 점유 · v15 픽스처 → `./.venv/bin/rcm serve --config …-l5-8815.toml; echo rc=$?` → `ls` → `sqlite3 … "PRAGMA user_version"` |
| L5.2 | **통과** | `rc=2` · 같은 1줄 `rcm: cannot start server: Address already in use` · 픽스처 `ls` = `rcm.sqlite3` 만 · `user_version` 15 · `curl -s :8814/api/health \| jq .ok` → **true**(도는 서버는 그대로) | 8814 설정의 사본에 `data_dir` 만 픽스처로(`…-l5-copy.toml`) → `rcm serve --config <사본>` |
| L5.3 | **통과** | 8815 를 풀고 띄우니 `[rcm] rcm 0.2.6 listening on http://127.0.0.1:8815 · lanes 1 · presets ok · data <HOME>/.local/share/rcm-verify-m5l-l5-old` · `/api/health` `{"ok":true,"db":true,"version":"0.2.6"}` · `backup/rcm.sqlite3.v15.bak` 생성 · `user_version` **16**. 내린 뒤 `lsof -iTCP:8815 -sTCP:LISTEN` 0줄 | `pkill -f "http.server 8815"` → `nohup rcm serve --config …-l5-8815.toml &` → 로그 · `ls backup` |
| L5.4 | **통과** | `rc=2` · 1줄 `rcm: cannot start server: Can't assign requested address` · 픽스처 `ls` = `rcm.sqlite3` 만 · `user_version` 15 | `bind = "10.255.255.1"` 사본(`…-badbind.toml`) → `rcm serve --config …` |
| L5.5 | **통과** | serve `rc=2` `rcm: cannot start server: file is not a database` · token list `rc=2` `rcm: file is not a database` · 둘 다 1줄 · `Traceback` 없음 · `lsof -nP -iTCP:8815 -sTCP:LISTEN` **빈 출력**(serve 가 잠깐 잡은 8815 는 종료로 풀렸다) | `printf 'garbage\n' > <data_dir>/rcm.sqlite3` |
| L5.6 | **통과** | serve `rc=2` `rcm: cannot start server: attempt to write a readonly database` · token list `rc=2` `rcm: attempt to write a readonly database` · 1줄씩 · 트레이스백 없음 | 현재 버전(16) DB · `-wal`·`-shm` 지움 · `chmod 500 <data_dir>`(`dr-x------`) → 둘 → `chmod 700` |
| L5.7 | **통과** | `token list` / `token add x` / `token revoke x` **셋 다** `rc=2` `rcm: File exists` · serve `rc=2` `rcm: cannot start server: File exists` · 넷 다 1줄 — `token` 도 `OSError` 를 잡는다 | `touch <HOME>/.local/share/rcm-verify-m5l-l5-file` 을 `data_dir` 로 |
| L5.8 | **통과** | `rcm: database is locked` · `rc=2` · **elapsed 5s**(`busy_timeout`) · 트레이스백 없음 | `python3 -c 'import sqlite3,time; c=sqlite3.connect("<db>"); c.execute("BEGIN IMMEDIATE"); time.sleep(12)' &` → 바로 `rcm token --config … add locked` |
| L5.9 | **통과(기록)** | stderr **2줄**: `warning: no [[presets]] configured — the server will accept nothing to run` + `rcm: cannot start server: database schema version 17 is newer than this build (16) — stop the service, restore <HOME>/.local/share/rcm-verify-m5l-l5-nopre/backup/rcm.sqlite3.v16.bak (and remove rcm.sqlite3-wal/-shm), or upgrade` · `rc=2`. 리뷰 B-5 대로 「한 줄」테스트는 프리셋 있는 설정으로만 돈다 — 오류 아님. (둘째 줄이 없는 `v16.bak` 을 가리키는 것은 이 브랜치가 아니라 #117(L1) 의 수정 범위 — dev 에 둘이 합쳐지면 `no migration backup found` 가 된다) | `[[presets]]` 절을 지운 설정 + `PRAGMA user_version=17` → `rcm serve --config … 2>err` |
| L5.10 | **통과** | `pytest tests/test_cli_serve_refusal.py -q` → **15 passed**. 있는 것: `test_a_taken_port_is_refused_before_the_database_is_opened`(소켓을 먼저 `bind`+`listen` · `-wal` 지운 v(V-1) DB · `main(["serve",…])` → `USAGE_EXIT` · `-wal` 없음 · `backup/` 없음 · `version_of(db) == V-1`) · `test_a_garbage_database_file_is_one_line`(serve / token list / add / revoke) · `test_a_read_only_data_dir_is_one_line`(serve / token list) · `test_a_data_dir_that_is_a_file_is_one_line_for_token_too`(serve / list / add / revoke). mutcheck ㉗ `serve-store-before-bind`(순서를 되돌린 변이) → `[OK ] control green, mutant red` · `grep -c "Mutant("` = **27** = AGENTS.md·CONTRIBUTING.md | 자동 |
| L5.11 | **통과** | CHANGELOG `[Unreleased]` `### Fixed`: 「takes its port before it opens the database … The socket is bound first; if that fails the database is not opened, not backed up and not changed」 · 「refuse in one `rcm:` line (exit 2) for every way the database cannot be opened — a file that is not a database, a read-only data directory, a migration statement that fails, a `data_dir` that is a file, another process holding the write lock — where before only a failed migration backup and a newer schema were one line」 · 「(`rcm gc --dry-run` keeps its exit 3 for the newer-schema case: it answers "unknown", the other two answer "fix the setup".)」(B-4 한 문장) · 링크 `[#118](…/pull/118)`. `[0.2.6]` 의 #94 항목(「refuse in one line when the database cannot be opened — a migration backup that could not be written, or a database a newer build has migrated」)은 릴리스된 절이라 그대로이고, `[Unreleased]` 항목이 「where before only … were one line and the rest was a traceback」으로 그 범위를 바로잡는다. `docs/operating.md` 「Upgrade」 7단계: 「The server takes its port before it opens the database, so a build started by mistake next to the running service stops at `Address already in use` without touching the database」 | `awk '/^## \[Unreleased\]/,/^## \[0.2.6\]/' CHANGELOG.md` · `grep -n 'cannot be opened' CHANGELOG.md` · `sed -n '105,112p' docs/operating.md` |
| LX.5 (8814) | **통과(기록)** | `curl -s :8814/api/status \| jq .schema_version` → **1** — v0.2.6 과 같다(CHANGELOG 「`schema_version` stays 1」) | 자동 |

## 오류와 수정

없음 — L5.1 ~ L5.11 전부 실제 모양의 입력으로 실제 경로를 지났다. 이 브랜치에 추가 커밋은 이 보고서뿐이다.

## 확인 못 함

없음.

## 비고

- 리뷰 B-4(같은 `newer_database_message` 가 `token`/`serve` 는 2, `gc --dry-run` 은 3)는 CHANGELOG `[Unreleased]` 의 괄호 문장으로 적혔다 — 바꾸지 않는다는 리뷰 결론 그대로.
- `scripts/mutcheck.py` 의 ㉗ 번호·27 개는 PR #117(L1)의 ㉗ `offline-gc-ignores-sqlite-error`·27 과 겹친다. 나중에 머지되는 쪽이 ㉘·28 로 맞춰야 한다(#118 본문 「머지 때 맞춘다」).
- PR #118 CI(2a8cf13): `secrets` · `smoke (macos-latest)` · `smoke (ubuntu-latest)` · `test` · `unit (macos-latest, 3.13)` · `unit (ubuntu-latest, 3.11, google-chrome)` · `unit (ubuntu-latest, 3.13, google-chrome)` 전부 pass.
