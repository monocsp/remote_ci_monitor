# M5l 검증 보고 — L6 태그 워크플로 · 2026-09-13 · PR #114 (`ci/tag-release-noop-when-tag-is-ancestor` @ b8c80a8)

- 시나리오: `docs/acceptance/m5l-verification-scenarios.md` §1 L6(리뷰 `docs/reviews/2026-09-13-impl-reviews/pr-85.md` B-1~B-4 · C-1 · C-3 · D). 실행 주체: 격리 에이전트(verify-l6).
- 워크트리 `../remote_ci_monitor-verify-m5l-l6`(`docs/acceptance-m5l-verify-l6`, 기준 `origin/ci/tag-release-noop-when-tag-is-ancestor` = b8c80a8) + 자기 `.venv`(python3.11 · `pip install -e '.[dev]'`).
- 실기 L6.1~L6.4 는 **레포의 태그를 만들지도 밀지도 않았다** — `tag-release.yml` 의 `id: tag` 스텝 `run` 블록을 정규식으로 `<data_dir>/tagstep.sh` 로 꺼내(`yaml` 은 dev extra 에 없어 정규식) 스크래치 레포(`git init s` + 로컬 bare `origin.git`)에서 돌렸다. `<data_dir>` = `<HOME>/.local/share/rcm-verify-m5l-l6`, 끝나고 지웠다. 서버 없음(8817 미사용).
- 스크래치 레포의 커밋·브랜치 이름도 `tools/guard_naming.py` 훅이 검사하므로 `chore(scratch): a` · `chore/scratch-other-commit` 꼴을 썼다(시나리오의 `-m a` · `-b other` 는 훅에 막힌다).

## 결과 요약

| ID | 판정 | 실측 |
|---|---|---|
| L6.1 | 통과 | rc **0** · `created=false` · 「tag v9.9.9 already points at d25d4f7…, an ancestor of 7ebb28f… — __version__ was not bumped, nothing to do」· `::error::` 없음 · origin 태그 불변(peeled d25d4f7 그대로) |
| L6.2 | 통과 | rc **1** · `::error::tag v9.9.9 already exists at 83433f9…, which is not an ancestor of 7ebb28f… — bump __version__ instead of reusing it` · `$GITHUB_OUTPUT` 비어 있음 · 태그 불변 |
| L6.3 | 통과 | rc 0 · `created=false` · 「already points at 7ebb28f…, an ancestor of 7ebb28f…」(같은 커밋은 자기 자신의 조상) |
| L6.4 | 통과 | rc 0 · `created=true` · origin `refs/tags/v9.9.9` → peeled `7ebb28f` = `GITHUB_SHA` · `git cat-file -t v9.9.9` = `tag`(주석 태그) · `git ls-remote --heads origin` 은 `master` 하나, 브랜치 push 없음 |
| L6.5 | 통과 | `fetch-depth: 0`(28행, 이유 주석) · `git merge-base --is-ancestor "$AT" "$GITHUB_SHA"`(49행) |
| L6.6 | 통과 | 워크플로 수준 `permissions:` **False** · `secrets: inherit` 는 주석(65행 「없다」)에만 · `test_tag_release_grants_contents_write_to_the_tag_job_only`(잡 수준으로 바뀜) · `test_tag_release_caller_permissions_cover_every_called_job` 초록 |
| L6.7 | 통과 | `release.yml` `ref: refs/tags/${{ env.TAG }}` 가 `build`(33) · `smoke`(93) · `github-release`(115) 셋 · `test_release_checks_out_the_tag_it_was_given` 이 셋을 돈다 |
| L6.8 | 통과 | CONTRIBUTING 「Releasing」 3항: 「A merge that does not bump `__version__` is a no-op (the tag already points at an ancestor of the merge commit)」 · 재실행은 Actions **Re-run** 또는 `git push origin :refs/tags/v0.1.0 && git tag -f v0.1.0 <main-sha> && git push origin v0.1.0` · 「`git push origin v0.1.0` alone is a no-op … `Everything up-to-date`」 |
| L6.9 | 통과 | `pytest tests/test_ci_release_workflow.py -q` **17 passed** · `bash -n tagstep.sh` ok · `test_tag_release_is_a_noop_when_the_tag_is_an_ancestor_and_refuses_otherwise` 가 `merge-base --is-ancestor` 문구를 잠근다 · `test_tag_step_*` 셋이 가짜 `git` 아래서 세 분기를 돈다 · CHANGELOG `[Unreleased]` Fixed 「A `main` merge that does not bump `__version__` no longer fails the `Tag release` run」 |
| L6.10 | 오너 대기 | `gh run list --workflow tag-release.yml` → **404 not found on the default branch**(main 은 아직 0.2.5 · b544567, `tag-release.yml` 없음) · `gh release view v0.2.6` → not found · 태그는 `v0.2.4` 까지 · 릴리스 PR #105(dev→main) 열려 있음. 첫 실행은 #105 머지 때 — 그 run 을 지켜보고 기록할 것 |

오류 0 · 수정 없음 · 확인 못 함 0(L6.10 은 기록 항목).

## 명령 그대로

```sh
# 스텝 추출(정규식) — <HOME>/.local/share/rcm-verify-m5l-l6/tagstep.sh, 머리에 set -u
python3 - .github/workflows/tag-release.yml tagstep.sh <<'PY'
import re, sys, textwrap
t = open(sys.argv[1]).read()
m = re.search(r"^ {8}id: tag\n(?:^ {8}\S.*\n|^ {10}.*\n)*?^ {8}run: \|\n((?:^ {10}.*\n|^\n)+)", t, re.M)
open(sys.argv[2], "w").write("set -u\n" + textwrap.dedent(m.group(1)))
PY
bash -n tagstep.sh
git init -q s && cd s && git commit -q --allow-empty -m "chore(scratch): a" && git commit -q --allow-empty -m "chore(scratch): b"
git init -q --bare ../origin.git && git remote add origin ../origin.git && git push -q -u origin HEAD
export TAG=v9.9.9 GITHUB_SHA=$(git rev-parse HEAD) GITHUB_OUTPUT=../out
# L6.1
git tag -a v9.9.9 -m x HEAD~1 && git push -q origin refs/tags/v9.9.9; bash ../tagstep.sh; echo rc=$?; cat $GITHUB_OUTPUT
# L6.2
git checkout -q -b chore/scratch-other-commit HEAD~1 && git commit -q --allow-empty -m "chore(scratch): c" \
  && git tag -f -a v9.9.9 -m x && git push -q -f origin refs/tags/v9.9.9 && git checkout -q -; bash ../tagstep.sh; echo rc=$?
# L6.3
git tag -f -a v9.9.9 -m x "$GITHUB_SHA" && git push -q -f origin refs/tags/v9.9.9; bash ../tagstep.sh; echo rc=$?
# L6.4
git push -q origin :refs/tags/v9.9.9 && git tag -d v9.9.9; bash ../tagstep.sh; echo rc=$?; git ls-remote --tags origin; git ls-remote --heads origin
```

실측 출력(sha 는 스크래치 레포의 것):

```
HEAD=7ebb28fd954d0edb99bef684981a00a3d361690b  HEAD~1=d25d4f7a690b21eb3b0d08911242e42e6c90ee31
[L6.1] tag v9.9.9 already points at d25d4f7a…, an ancestor of 7ebb28fd… — __version__ was not bumped, nothing to do
       rc=0  created=false
[L6.2] ::error::tag v9.9.9 already exists at 83433f99…, which is not an ancestor of 7ebb28fd… — bump __version__ instead of reusing it
       rc=1  (GITHUB_OUTPUT 비어 있음)
[L6.3] tag v9.9.9 already points at 7ebb28fd…, an ancestor of 7ebb28fd… — __version__ was not bumped, nothing to do
       rc=0  created=false
[L6.4] To ../origin.git  * [new tag]  v9.9.9 -> v9.9.9 / created tag v9.9.9 at 7ebb28fd…
       rc=0  created=true  · refs/tags/v9.9.9^{} = 7ebb28fd… · heads: master 만 · cat-file -t = tag
```

문서·자동 행:

```sh
grep -n "fetch-depth\|merge-base --is-ancestor" .github/workflows/tag-release.yml     # 28 · 49
python3 -c 'import re; t=open(".github/workflows/tag-release.yml").read(); print(bool(re.search(r"^permissions:", t, re.M)))'   # False
grep -n "secrets: inherit" .github/workflows/tag-release.yml                           # 65(주석뿐)
grep -n "ref: refs/tags" .github/workflows/release.yml                                 # 33 · 93 · 115
sed -n '/## Releasing/,/^## /p' CONTRIBUTING.md
./.venv/bin/pytest tests/test_ci_release_workflow.py -q                                # 17 passed
gh run list --workflow tag-release.yml -L 5 ; gh release view v0.2.6                    # 404 · not found
```

## 비고

- 리뷰 #85 B-1 의 재현 조건(「태그 `vX` 가 있는 상태에서 `__version__` 그대로인 커밋을 main 에 push」)이 L6.1 이고, 수정 후 무동작이다. 진짜 재사용(L6.2)만 거절한다.
- L6.10 은 v0.2.6 이 main 에 오른 뒤 첫 `Tag release` run(태그 · Release · 노트)을 이 보고 또는 M5i 보고에 추가로 적는다.
