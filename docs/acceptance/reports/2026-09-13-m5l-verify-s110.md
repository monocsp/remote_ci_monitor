# M5l 검증 보고 — S5~S10 취소 capability 보완 (2026-09-13)

- 대상: PR #110 `feat/cancel-submission-capability` · HEAD **`f65737d`**(리뷰 보완 커밋 `fix(server): 취소 capability 리뷰 보완 — 세션별 조회·leave 바인딩·원자성` 이 올라간 뒤) · 리뷰 `docs/reviews/2026-09-13-impl-reviews/pr-110.md` · 명세 `docs/m5l-review-supplement-workplan.md` §1 S5~S10
- 근거: `docs/acceptance/m5l-verification-scenarios.md` §0 · §1 S5~S10 · LX.3(8812) · LX.4 · LX.5(8812) 와 `docs/acceptance/m5j-verification-scenarios.md` §1 G5 중 S5~S10 이 건드린 행(G5.3 · G5.4 · G5.5 · G5.6~G5.10 · G5.13~G5.15 · G5.17 · G5.18)
- 워크트리 · 브랜치: `../remote_ci_monitor-verify-m5l-s110` · `docs/acceptance-m5l-verify-s110`(PR 브랜치에서 분기, 결과는 PR 브랜치로 push) · 옛 클라: `../remote_ci_monitor-verify-old-s110`(dev 의 `release: 버전 0.2.6` = `73584b5`) 의 자기 venv `~/.local/share/rcm-verify-m5l-s110-oldvenv`(`__version__ 0.2.6`, capability 코드 없음)
- 시험 서버: **8812** 키 on(`cancel_requires_submission_token = true`, `~/.local/share/rcm-verify-m5l-s110`) · **8813** 키 off(`…-s110-off`) — S10.4 의 「8812 를 off 로 재기동」 대신 off 서버를 따로 두었다. 둘 다 `bind 127.0.0.1` · `advertise = false` · `lanes = 1` · `[notify]` 없음 · 프리셋 `ok`·`sleepy`. 클라이언트는 전부 `XDG_STATE_HOME=<data_dir>/state`, `--config` 없는 `rcm check` 는 `HOME=<data_dir>/home`. 끝나고 서버를 내리고 `data_dir`·옛 venv·옛 워크트리를 지웠다
- 토큰: alice(`$A`) · bob(`$B`) · charlie(`$C`) 일반, root(`$R`) admin. 비밀은 앞 4자만, 절대경로는 `<HOME>` 으로 적는다
- 판정 요약: **통과 40 · 오류 2(S9.7 문서 → 이 브랜치에서 수정 · S10.6 웹 admin 예외 → 오너) · 오너 결정 2(S5.3 · S10.3 옛 클라 절반) · 보류(명세 밖) 1(S10.7) · 확인 못 함 0**. 코드(src) 는 고치지 않았다 — `docs/configuration.md` 한 단락만 보탰다

## 1. 결과 표

| ID | 판정 | 실측 | 명령 / 비고 |
|---|---|---|---|
| S5.1 | 통과 | 셸 A `rcm run sleepy --dir tree1 --no-wait --token $A` → 잡 #1 `joined false` · `submission.id 4dc0…`(`cancel_job`) · 셸 B 같은 트리 같은 토큰 → `joined true` · `submission.id 55cc…`(`leave_submission`) · 상태 파일에 두 항목(지문 같음) · 셸 B `rcm cancel 1 --token $A` → **200 `{"left":true,"job_id":1,"job_state":"running"}`** exit 0 · `GET /jobs/1.state running` · `SELECT submission_id, role FROM submissions WHERE job_id=1` → `4dc0… cancel_job` 한 행 · 상태 파일에 SA(`cancel_job`) 만 남음 | 리뷰 #110 B P1 첫째 재현 — 이제 합류 세션은 자기 leave 만 |
| S5.2 | 통과 | 셸 A `rcm cancel 1 --token $A` → **200 `{"job_id":1,"state":"cancelling"}`** → `cancelled` · `cancelled_by alice` · 쓴 비밀은 파일에서 지워져 항목 0 | |
| S5.3 | **오너 결정** | 잡 #2: A 제출(`47a6…` cancel_job) · B 합류(`2909…` leave) · **A 가 먼저** `rcm cancel 2 --token $A` → **200 `left: true`**(cancelling 이 아니다) · joiners `[]` · 잡 running. `rcm cancel 2 --token $A --submission-id 47a6…` → **200 `cancelling`** → `cancelled` | 구현의 규칙은 「내 지문의 항목 중 그 잡의 **가장 최근** 제출」(`submissions.py` 모듈 docstring · `docs/usage.md` · `configuration.md` 에 적혀 있음). 같은 파일·같은 토큰의 두 셸을 가를 정보가 없으니 S5.1(B → leave)과 S5.3(A → cancelling)을 **동시에** 만족시키는 규칙은 없다 — 구현은 「나가기」라는 안전한 쪽 + `--submission-id`. §4 ① |
| S5.4 | 통과 | 상태 파일에 `server http://127.0.0.1:8899` · 같은 `job_id 3` 의 가짜 항목(비밀 `FAKE…`) 추가 → `rcm cancel 3 --token $A` → **200 `cancelling`** → `cancelled` · 서버 로그에 `403`·`FAKETOKEN` 0건 · 가짜 항목은 그대로 남고(다른 서버 것이라 안 건드린다) 8812 항목만 지워짐 | 항목은 `server` 로 먼저 가른다 |
| S5.5 | 통과(기록) | `lookup.__doc__`: 「같은 서버·같은 잡·같은 토큰 지문의 cancel token — `submission_id` 를 주면 그 항목(지문은 안 본다), 아니면 가장 최근 항목. **다른 지문의 항목으로는 넘어가지 않는다**」 · `grep -n fingerprint submissions.py` → 지문 필드는 남아 있고 `_find` 가 **같은 지문만** 거른다(206행). 다른 지문으로의 폴백도 `cancel_job` 우선순위도 없다 | 지문이 「선택에 안 쓰인다」는 아니고 **필터**로 쓰인다 — 커밋 본문이 이유를 적었다(다른 토큰의 세션이 합류한 뒤 요청자가 취소하는 e2e 가 그것을 요구). 목적(리뷰 B P1 첫째)은 닫혔다 |
| S5.6 | 통과 | `pytest tests/test_cancel_capability.py tests/test_cancel_capability_m5l.py -q` → 34 passed · 옛 테스트(`e83b7ff`) 는 같은 토큰의 두 번째 세션 뒤 `lookup(…, token_fingerprint=A) == "newer"`(요청자 비밀이 이긴다)를 고정했고, `f65737d` 가 `test_the_state_file_is_written_atomically_and_prunes_only_finished_jobs` 에서 **`lookup(...) == "alice-leave"`** · `submission_id="s7"` 면 `"newer"` 로 바꿨다 · e2e 는 `test_s5_a_second_session_of_the_same_token_cancels_its_own_join_not_the_job`(`left: true`) | |
| S5.7 | 통과 | `docs/usage.md` 158~166 · `usage.ko.md` 159 · `configuration.md` 455~463 · CHANGELOG `[Unreleased]` 24~26: 「같은 client token 이라도 다른 상태 디렉터리면 못 한다 · 같은 사용자의 두 세션은 파일을 공유하고 `rcm cancel N` 은 이 토큰의 **가장 최근** 제출을 보낸다 · `--submission-id`」 | 「같은 상태 디렉터리라도」가 붙어 있다 |
| S6.1 | 통과 | 잡 #4: alice 제출 · bob(`SFPL…`) · charlie 합류 → charlie bearer + bob 비밀 `curl -X POST … -d '{"cancel_token":"$CAP_B"}'` → **403 `cancel_token_mismatch`**(`not your submission — that cancel token belongs to another participant`) · joiners `[bob, charlie]` 그대로 · `submissions` 세 행(`cancel_job alice` / `leave_submission bob` / `leave_submission charlie`) 그대로 | 리뷰 #110 B P1 둘째 재현 |
| S6.2 | 통과 | 같은 요청을 bob bearer 로 → **200 `{"left":true,…,"job_state":"running"}`** · joiners `[charlie]` · bob 행만 삭제 · 잡 running | |
| S6.3 | 통과 | 새 잡 20개(`t6_1…t6_20`), 각각 bob 합류 뒤 같은 leave 요청 둘을 `&` 로 동시에 → 20회 전부 **`200` 하나 + `403` 하나** · joiners 에서 bob 한 번 빠짐 · 서버 로그 `Traceback` 0 | 자동: `test_s6_the_same_leave_secret_sent_twice_at_once_succeeds_exactly_once`(6개 동시 → 200 하나) |
| S6.4 | 통과 | S6.2 뒤 같은 `$CAP_B` 다시 → **403 `cancel_token_required`**(행이 없으니 비밀 없는 요청과 같다) | |
| S6.5 | 통과(다른 이름) | 서버는 이제 `remove_submission` 을 leave 경로에서 안 부른다 — 행 확인·삭제·joiner 삭제가 `Store.consume_leave(job_id, submission_id, participant, drop_joiner)` 의 `BEGIN IMMEDIATE` 한 트랜잭션이고 False 면 **403 `cancel_token_used`**(`server.py` 1817~1829). 잠금: `test_s6_store_consume_leave_checks_the_row_and_removes_the_joiner_in_one_transaction`(다른 참여자 이름·없는 id·두 번째 호출 → False, joiner 유지) + S6.3 의 동시 테스트(5개가 False → 403) | monkeypatch 대상이 바뀐 것이지 「결과 확인」은 있다 |
| S6.6 | 통과 | `PRAGMA table_info(submissions)` → `job_id · submission_id(PK) · role · capability_hash · created_at · participant(NOT NULL)` · `PRAGMA user_version` **17** · `DB_VERSION = 17` · `_MIGRATIONS[17]` 은 `CREATE TABLE IF NOT EXISTS submissions (… participant TEXT NOT NULL)` + 인덱스(제자리 DDL, 18 없음) · `pytest -k v16` → `test_v16_to_v17_takes_the_boundary_backup_first_and_a_failed_backup_adds_no_table` 초록 | |
| S6.7 | 통과 | `python scripts/mutcheck.py --only leave-binding-removed` → `[OK ] leave-binding-removed: control green, mutant red` · `mutcheck.py` 의 `Mutant(` 28개 = AGENTS.md 「28 known mutations」 | |
| S7.1 | 통과 | 상태 파일 비운 뒤 `rcm run ok` 둘씩 `&` 로 20라운드(트리 40개) → 40개 전부 exit 0 · `jq length` **40** · `job_id` 고유 **40**(#25~#64 빠짐 없음) · `ls state/rcm/` → `submissions.json` · `submissions.json.lock` 뿐(`.tmp` 없음) · 파일 유효 JSON | 리뷰 #110 B P1 셋째 재현 |
| S7.2 | 통과 | `submissions.py` 78 `fcntl.flock(fd, fcntl.LOCK_EX)`(`<file>.lock` 에) 아래 `_load → 수정 → _write` · 86 `tmp = path.with_name(f"{path.name}.{os.getpid()}.tmp")` + `os.replace` | |
| S7.3 | 통과 | 다른 프로세스가 `submissions.json.lock` 을 `LOCK_EX` 로 3초 쥔 0.3초 뒤 `rcm run ok --no-wait` → **exit 0 · 2.75초**(락 해제 시점에 맞춰 끝남 — 락 없이는 1초 미만) · 항목이 파일에 있음(41) · 건너뛴 경고 없음 | 자동: `test_s7_remember_waits_for_the_lock_and_names_its_temp_file_by_pid` |
| S7.4 | 통과 | `stat -f '%Lp'` → 파일 **600** · 디렉터리 **700** | |
| S7.5 | 통과 | `test_s7_two_processes_writing_the_state_file_at_once_lose_nothing`(`multiprocessing` · 프로세스 넷 · 100 항목) | 리뷰 C 「순차 쓰기뿐」 → 실제 다중 프로세스 |
| S8.1 | 통과 | `test_s8_a_failed_capability_insert_leaves_no_job_row_and_no_join` — `sqlite3.OperationalError("database or disk is full")` 주입 → 예외 · 잡 행 0 | |
| S8.2 | 통과 | 같은 테스트의 합류 절반 — joiner·join_count·`submissions` 불변 | |
| S8.3 | 통과 | `rcm run ok --no-wait` → 잡 #67 · `SELECT count(*) FROM submissions WHERE job_id=67` → **1** | |
| S8.4 | 통과 | `server.py` 에 `add_submission` 호출 없음(1318 · 1665 `join_or_bump_with_submission`, 1330 · 1678 `create_job_with_submission`) · 둘 다 `store.py` 에서 `BEGIN IMMEDIATE`(1501 · 1583) 안에 잡/joiner 와 capability 행을 같이 만든다 | |
| S9.1 | 통과 | `rcm pause` 뒤 `rcm run ok --no-wait` 201개(트리 201) → 전부 exit 0 · `jq length` **201**(그 전에 있던 끝난 잡 항목 43개만 밀리고 활성 201개 전부 보존, 첫 잡 #68 부터) | 명세 S9 「201개 활성 → 전부 보존」 |
| S9.2 | 통과 | `rcm cancel 68 --token $A` → **200 `cancelled`**(대기 잡) · 파일 200 | |
| S9.3 | 통과 | `rcm resume` → 4초 뒤 queued+running 0 · 새 sleepy 잡 #269 → `jq length` **200** · 항목 `job_id` 는 #70~#269(running 인 #269 포함 — 끝난 #69 하나만 밀렸다) | |
| S9.4 | 통과 | 8812 를 내린 채 항목 201개 파일 → `rcm run ok --no-wait` → `rcm: cannot read presets: cannot reach http://127.0.0.1:8812: [Errno 61] Connection refused` **exit 2** · `jq length` **201** 불변 · `_prune(entries, None)` · `finished` 가 None/예외 → 201 그대로 | 자동: `test_s9_the_cap_prunes_only_finished_jobs_and_keeps_unknown_ones` · `test_s9_rcm_run_asks_the_server_which_jobs_finished_before_pruning`. 연결 실패의 exit 는 3 이 아니라 2 였다(기록) |
| S9.5 | 통과 | `chmod 500 state/rcm` → `rcm run ok --no-wait` exit 0 + stderr 한 줄 `rcm: warning: the cancel token was not saved in the rcm state file (Permission denied) — from another shell, \`rcm cancel 270\` needs --cancel-token (the --no-wait JSON carries it)` · stdout JSON 에 `submission.cancel_token` 있음(43자) · stderr 에 비밀 0건 · `--poll` 도 같은 경고 한 줄, exit 0 · 두 stderr 에 `/Users/` 0건 | 자동: `test_s9_a_failed_state_save_says_so_without_a_path_and_the_json_still_has_the_token` |
| S9.6 | 통과 | `chmod 700` 뒤 상태 파일 없이 `rcm cancel 272 --token $A --cancel-token <JSON 의 값>` → **200 `cancelling`** | |
| S9.7 | **오류 → 수정** | `docs/configuration.md` 「Who may cancel a job」에 상한(200)·「끝난 잡만 정리」·「저장 실패면 `--cancel-token`」이 **없었다**(`usage.md` 172 · `usage.ko.md` 164 · CHANGELOG 29 에는 있음). 이 브랜치에 한 단락 보탬 — §2 | 리뷰 D 「문서에 없다」 |
| S10.1 | 통과 | alice 잡 #273 · `UPDATE submissions SET role='root_everything'` → `rcm cancel 273 --token $A` → `rcm: cancel failed: not your submission` **exit 2**(403 `cancel_token_role`) · 잡 running · 서버 로그 `Traceback` 0 · 로그 한 줄 `[rcm] cancel job #273: submission aaa4… has unknown role 'root_everything' — refused`(비밀 없음) | 자동: `test_s10_an_unknown_role_in_the_table_is_refused_and_logged` |
| S10.2 | 통과 | 상태 파일 삭제 → `rcm cancel 274 --token $A 2>err.txt`(`no cancel token for job #274: none saved in the rcm state file and no --cancel-token — sending the cancel without one` + `cancel failed: … --cancel-token …`) · `chmod 500` 뒤 `rcm run … --no-wait 2>>err.txt` → `grep -c "/Users/\|$HOME" err.txt` → **0**(「rcm state file」로만 부른다) | 자동: `test_s10_stderr_never_prints_the_state_file_path` |
| S10.3 | 통과(새 클라) · **오너 결정(옛 클라)** | 8812 health `min_client_version "0.2.7"` · `cancel_min_client_version "0.2.7"` · `version "0.2.6"`. **새 클라** `rcm check` → `FAIL client v0.2.6 · server v0.2.6 · below min client v0.2.7 — pip install …whl` + `ok cancel needs the submission's cancel token · min client v0.2.7 (this client sends one)` **exit 1**(같은 버전 번호라도 바닥을 먼저 본다 — `_client_row` 수정 확인). **옛 0.2.6 클라** `rcm check` → `ok client v0.2.6 · same as server` **exit 0** — 옛 클라 자기 코드(`73584b5` `cli.py` 345)가 버전이 같으면 `min_client_version` 을 안 보고 끝낸다. 서버 `version` 이 0.2.6 인 한 PR 에서 못 고치고, `__version__` 0.2.7 상승(릴리스) 뒤에는 「older」 경로로 FAIL 이 된다 | 리뷰 B P2 다섯째의 「0.2.7 버전 상승 후에는 사라진다」 그대로. 새 클라의 `rcm run` 도 그때까지 매번 `warning: this client v0.2.6 is older than the server accepts (min_client_version 0.2.7)` 를 찍는다(기록). 자동: `test_s10_client_row_fails_below_the_floor_even_when_the_versions_are_equal` |
| S10.4 | 통과 | 8813(off) health `min_client_version "0.2.0"` · `cancel_min_client_version null` · 옛 클라 `rcm check` → `ok client v0.2.6 · same as server` exit 0 · 새 클라도 ok exit 0(경고 없음) | 8812 재기동 대신 off 서버 |
| S10.5 | 통과 | `-k "role or unknown"` 3 passed · `-k "path or tilde or stderr"` 2 passed · `-k min_client` 는 **0건**(이름이 다름) — 같은 것을 `test_s10_client_row_fails_below_the_floor_even_when_the_versions_are_equal` · `test_rcm_check_mentions_the_cancel_floor_only_when_the_key_is_on` 이 잠근다 | |
| S10.6 | **오류(오너)** | Chrome(`tests/test_web_browser.Chrome`, `/Applications/Google Chrome.app`) · 8812 · alice 잡 #287(bob 합류) · `/?poll=1&lang=en`: alice → `[data-cancel="287"]` **disabled** + 「cancelling needs the submission's cancel token — run rcm cancel in the session that submitted, or use an admin token」 · **root(admin) → disabled + 「not your job」** · bob → disabled + 같은 잠금 문구 · `page_errors() == []` 셋 다 | `app.js` 1498 · 1594 `canAct = … && (mine \|\| state.me === null)` 에 admin 이 없다(리뷰 A-4). `configuration.md` 490~491 「admin 토큰이면 버튼 활성」은 남의 잡에선 거짓. 명세 표 밖이라 시나리오대로 오너에게 — §4 ② |
| S10.7 | 보류(명세 밖) | bob 이 `rcm run sleepy --no-wait` 로 합류한 잡 #284 에서 `rcm wait --job 284 --token $B` + SIGINT → exit 3 · `detached: true` · **`left: false`** · joiners `[bob]` 그대로 · bob 의 `leave_submission` 행 그대로 | 리뷰 A-3 그대로(독립 `rcm wait` 의 Ctrl-C 는 참여를 안 뺀다). `run --poll` 의 Ctrl-C 는 G5.15 대로 뺀다 |
| G5.3 | 통과 | 잡 #276: alice(`34e3…` cancel_job) · bob(`b6c6…` leave) · 같은 토큰 alice 두 번째 셸(`bf25…` leave) — 셋 다 id·비밀 다름 · `submissions` 세 행 `cancel_job alice / leave_submission bob / leave_submission alice` | |
| G5.4 | 통과 | 8813(off) 잡 #1 alice + bob 합류 · ① bob curl 취소(`-d '{}'`) → **200 `left: true`** · joiners `[]` · running ② 옛 0.2.6 venv `rcm cancel 1 --token $A` → **200 `cancelling`** exit 0 → `cancelled` | 키 off 는 0.2.x 호환 |
| G5.5 | 통과 | S10.4 와 같다 | |
| G5.6 | 통과 | 8812 alice 잡 #286 · 요청자 토큰 curl `-d '{}'` → **403 `cancel_token_required`**(해시·경로 없음) · 잡 running · `grep -c <비밀> server.log` 0 | 본문 없는 `POST` 는 411 `Content-Length is required`(기록 — `-H "Content-Length: 0"` 또는 `-d '{}'`) |
| G5.7 | 통과 | `rcm cancel 276 --token $A --cancel-token <alice 것>` → **200 `cancelling`** → `cancelled` · `cancelled_by alice` | |
| G5.8 | 통과 | bob `rcm cancel 276 --token $B --cancel-token <bob 것>` → **200 `left: true`** · `left the join list of job #276 (job keeps running)` · joiners `[]` · alice 행 남음 · running | |
| G5.9 | 통과 | ① 마지막 글자 바꾼 비밀 ② 다른 잡 #277 의 유효 비밀 ③ 떠난 bob 의 비밀 재사용 → 셋 다 **403 `cancel_token_required`**(같은 본문, 힌트 없음) · 잡 running · joiners `[]` · 표 2행 불변 | |
| G5.10 | 통과 | `rcm cancel 277 --token $R`(상태 파일 항목 없음) → `no cancel token … sending the cancel without one` + **200 `cancelling`** | |
| G5.13 | 통과 | 상태 파일 삭제 → `rcm cancel 278 --token $A` → stderr `… no --cancel-token …` + `rcm: cancel failed: not your submission — pass the cancel token from the submission (rcm cancel --cancel-token …)` **exit 2** · 잡 그대로 | |
| G5.14 | 통과 | alice `rcm run sleepy --poll` 6초 뒤 SIGINT(비대화형 셸의 `&` 는 SIGINT 를 무시해 Python `Popen.send_signal` 로) → **exit 3** · stderr `detached from job #284 — it keeps running. Resume with \`rcm wait --job 284\`; stop it with \`rcm cancel 284\`.` · 마지막 JSON `detached: true` · 잡 running · 서버 로그에 #284 cancel 요청 0 · 상태 파일에 `cancel_job` 항목 남음 | |
| G5.15 | 통과 | bob 같은 트리 `--poll` 합류 뒤 SIGINT → exit 3 · `left: true` · stderr 끝 `(left the join list)` · joiners `[]` · bob 의 `submissions` 행 삭제 · alice 행 그대로 | 상태 파일의 bob `leave_submission` 항목은 남는다(`forget` 은 `rcm cancel` 만 부른다 — 죽은 항목, 해가 없다 · 기록) |
| G5.17 | 통과 | 8812 health `min_client_version "0.2.7"` = `CANCEL_MIN_CLIENT_VERSION` · 8813 `"0.2.0"` · 문서 `configuration.md` 482~488 「키를 켜면 옛 클라의 취소가 403」 | `rcm check` 의 client 행은 S10.3 |
| G5.18 | 통과 | 옛 클라 → 8812: `rcm run ok --no-wait` → JSON `job_id 285 · joined false · state queued · url`(넷 다) 제출 됨 · `rcm run sleepy --no-wait` 뒤 옛 `rcm cancel` → `rcm: cancel failed: not your submission — pass the cancel token …` **exit 2** | `rcm check` 는 S10.3 참고 |
| LX.3 | 통과(L8 제외) | `curl /api/status`(8812 · 8813) · 모든 `GET /jobs/<id>`(291개) · `rcm jobs --json` · `<data_dir>/logs/` · stderr 모음 7개(`err.txt s95a s95b g514 g515 s107 s73`) 에 `grep -rn "$HOME\|/Users/\|/opt/homebrew"` → 서버 로그 기동 배너 `data <HOME>/…` 한 줄씩과 **`/api/status` 의 `pools[0].hosts[0].disk.path`** 뿐(L8 · 오너 대기 ①, 이 PR 밖) · stderr 7개 전부 0 | |
| LX.4 | 통과 | `strings rcm.sqlite3 rcm.sqlite3-wal \| grep -c <비밀>` **0** · `server.log` 0 · `/api/status` 0 · `capability_hash` 291행 전부 **64자 hex** · 로그 URL 에 `cancel_token=` 0 | |
| LX.5 | 통과 | 8812 · 8813 `schema_version` **1**(`origin/dev` 의 `SCHEMA_VERSION = 1` 과 같음) · `server` 키에 `cancel_requires_submission_token` 추가만 | |

## 2. 오류와 수정

| 항목 | 무엇 | 수정 | 검사 |
|---|---|---|---|
| S9.7 | `docs/configuration.md` 「Who may cancel a job」이 상태 파일의 상한(200)·「끝난 잡만 정리」·「저장 실패면 `--cancel-token`」을 말하지 않았다 — 리뷰 D 의 지적이 `usage.md`·CHANGELOG 에만 반영됐다 | 같은 단락에 「A token that worked is removed from the file. The file keeps at most 200 entries; above that it drops only entries whose job the server says has finished … when the file cannot be written (`rcm run` prints one `warning: …` line, never the token itself) the `--no-wait` JSON is the only copy, and `rcm cancel N --cancel-token …` is the way to use it.」 을 보탰다 — 이 보고서와 같은 브랜치의 커밋 `docs(configuration): …` | `ruff check .` · `ruff format --check .` · 전체 `pytest` exit 0(doc-lock 포함) · src 무변경이라 mutcheck 는 `--only leave-binding-removed` 만 |

## 3. 확인 못 함

없다. Chrome 이 있었고(S10.6), 옛 0.2.6 클라는 `73584b5` 워크트리의 venv 로 만들었고, 경쟁 창(S6.3 · S7.1 · S7.3)은 전부 맞췄다.

## 4. 오너 결정

| # | 항목 | 실측 | 갈림길 |
|---|---|---|---|
| ① | **S5.3** — 같은 토큰·같은 상태 파일의 두 셸 중 **요청자** 셸이 먼저 `rcm cancel N` 을 하면 | `left: true`(합류 항목이 더 최근이라). `--submission-id <SA>` 를 주면 `cancelling` | (a) 지금대로 — 「가장 최근 제출」 + `--submission-id`(문서 세 곳이 이미 이렇게 적혀 있다 · 안전한 쪽으로 틀린다) (b) 같은 지문의 항목이 그 잡에 둘 이상이면 **모호하다고 거절**하고 `--submission-id` 후보를 보여 준다(S5.1 도 플래그가 필요해진다) (c) 시나리오 S5.3 의 기대(요청자 셸 → cancelling)를 지키려면 셸을 가르는 표지가 필요한데 없다 — 기대를 (a) 로 고친다 |
| ② | **S10.6** — 키 on 서버에서 admin 토큰으로 본 **남의** 잡의 웹 취소 버튼 | disabled + 「not your job」(`canAct` 에 admin 이 없다 — 리뷰 A-4). API 는 admin 의 남의 잡 취소를 허용한다(G5.10) | (a) `app.js` 의 `canAct`·`canActRow` 에 `state.admin` 을 더해 문서(`configuration.md` 490~491)·API 와 맞춘다 — 이 PR 또는 후속 (b) 문서를 「admin 도 자기 잡만」으로 고친다. 어느 쪽이든 지금은 문서가 거짓이다 |
| ③ | **S10.3** — 옛 0.2.6 클라의 `rcm check` 가 0.2.6 strict 서버에서 `same as server` OK | 옛 클라 코드의 문제라 PR 이 못 고친다. `__version__` 0.2.7 상승(릴리스 절차) 뒤엔 FAIL | 릴리스 순서(명세 §3) 그대로면 저절로 닫힌다 — 별도 결정 없이 기록만. 단 그때까지 새 클라(0.2.6 코드)도 strict 서버에서 `rcm check` FAIL · `rcm run` 경고를 낸다 |
| ④ | **S10.7** — 독립 `rcm wait --job N` 의 Ctrl-C(리뷰 A-3) | `left: false`, 참여 유지 | 명세 밖(M5l 표에 없다) — PLAN 의 `rcm wait` 계약대로 하려면 `rcm wait` 가 상태 파일의 `leave_submission` 항목을 쓰게 한다. 후속으로 |
