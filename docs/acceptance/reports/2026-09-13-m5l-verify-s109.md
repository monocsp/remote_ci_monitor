# M5l 검증 보고 — S1~S4 프리셋 `requires` 보완 (PR #109)

- 날짜: 2026-09-13
- 검증 대상: PR #109 `feat/preset-requires-tools-preflight` HEAD **`d471633`**
  (`fix(worker): 프리셋 requires 보완 — 워크스페이스 PATH·이름만 로그·취소 우선`). 오류 수정은 별도 PR 이 아니라
  **같은 PR 브랜치에 커밋**했다(`31fbf5e`, 아래 「오류와 수정」).
- 명세: `docs/acceptance/m5l-verification-scenarios.md` §0 규칙 · §1 S1.1~S4.5 · LX.3(8811) · LX.5(8811) ·
  `docs/m5l-review-supplement-workplan.md` S1~S4 · `docs/reviews/2026-09-13-impl-reviews/pr-109.md` §B ·
  `docs/acceptance/m5j-verification-scenarios.md` §1 G4 의 행 중 S1~S4 가 바꾼 것(G4.2 · G4.3 · G4.5 · G4.10 · G4.13 ·
  G4.14 · G4.15① · G4.16 · G4.18 — 아래 「G4 재확인」).
- 워크트리: `remote_ci_monitor-verify-m5l-s109` (`docs/acceptance-m5l-verify-s109`, PR 브랜치 기반) · 자기 `.venv`
  (Python 3.11.15). 운영 체크아웃·`~/.config/rcm`·`~/.local/share/rcm`·`~/.local/state/rcm` 은 열지 않았다.
- 시험 서버: 127.0.0.1:**8811** · `~/.local/share/rcm-verify-m5l-s109.toml` · `data_dir = "~/.local/share/rcm-verify-m5l-s109"`
  · `advertise = false` · `[notify]` 없음 · `lanes = 1` · 프리셋 아홉(`ok` `sleepy` `needs-fvm-rel` `needs-fvm-empty`
  `needs-fvm-rel-remote`(pool linux) `needs-missing` `needs-missing-remote`(pool linux) `needs-abs-true`(`/usr/bin/true`)
  `needs-missing-abs`(`/usr/local/rcm-verify-missing-9f3a`)). 서버는 **cwd `<data_dir>/srvcwd`** 에서 절대경로
  `<워크트리>/.venv/bin/rcm serve --config …` 로 띄웠다(S1 의 전제). 토큰 alice `Zn18…`(일반) · root `dWYJ…`(admin) ·
  wk `FNVh…`(worker). 원격 워커는 같은 Mac 에서 `--data ~/.local/share/rcm-verify-m5l-s109-worker/data`, cwd
  `…-worker/cwd`(안에 `tools/fvm`). 서버 셸 `echo $PATH` 에 `tools`·`srvcwd` 항목 없음. 끝나고 서버를 내리고
  `data_dir` · 워커 디렉터리 · 설정 파일 · 제출 트리를 지웠다.
- 제출: `treeA/`(`f` 하나) · `treeB/`(`f` + `tools/fvm`) · `treeC~E/`(내용 다른 `f` — 합류 방지) ·
  `treeMany/`(2 KB 파일 60,000개 — S4 의 자재화 창용). `$RCM` 은 워크트리의 `./.venv/bin/rcm`, `$S` 는
  `http://127.0.0.1:8811`. 파수꾼: 프리셋 argv 가 `touch "$RCM_WORKSPACE/../../sentinel-$RCM_JOB_ID"` 를 한다 —
  프로세스가 떴는지의 증거는 로그가 아니라 이 파일이다.

## 판정표

| ID | 판정 | 실측 | 명령/비고 |
|---|---|---|---|
| S1.1 | **통과** | `srvcwd/tools/fvm` 있음 · `treeA` 에 없음: `rcm run` exit **1** · 마지막 JSON `{"id":1,"state":"failed","summary_code":"tool_missing","summary_args":{"tool":"fvm"},"exit_code":null,"summary":"required tool fvm is missing"}` · `ls <data_dir>/sentinel-*` → no matches(**파수꾼 없음**) · 잡 로그 `[rcm] required tool fvm: missing` | `$RCM run needs-fvm-rel --server $S --token $A --dir treeA --poll` |
| S1.2 | **통과** (구현 **A**) | `srvcwd/tools/` 를 비우고 `treeB`(안에 `tools/fvm`): exit **0** · `{"id":3,"state":"succeeded","exit_code":0}` · `sentinel-3` **있음** · 로그 `[rcm] required tools: fvm ok` — 상대 항목을 **워크스페이스 기준으로 푼다**. `docs/configuration.md` 「A relative `PATH` entry (`tools`, `.`, or an empty entry between two colons) means the **job's workspace**」가 같은 말을 한다. S1.1 과 함께: 서버 cwd 의 파일을 본 적이 없다 | `$RCM run needs-fvm-rel … --dir treeB --poll` |
| S1.3 | **통과** | `srvcwd/fvm`(cwd 바로 아래) 있음 · `PATH = ":/usr/bin:/bin:"`: exit **1** · `{"id":2,"state":"failed","summary_code":"tool_missing","summary_args":{"tool":"fvm"},"exit_code":null}` · 파수꾼 없음 — 빈 항목이 서버 cwd 로 풀리지 않는다 | `$RCM run needs-fvm-empty … --dir treeA --poll` |
| S1.4 | **통과** | 워커 cwd 에 `tools/fvm` 을 두고 `--once`: exit 0 · stdout `lane 1: claimed #4 needs-fvm-rel-remote` → `lane 1: #4 failed — required tool fvm is missing` · `GET /jobs/4` = `{"state":"failed","summary_code":"tool_missing","summary_args":{"tool":"fvm"},"exit_code":null,"failed_step":null,"last_step":null,"failures":[]}` · `find …-worker -name 'sentinel-*'` → 없음 · 워커 stdout/stderr 에 `tools`·`srvcwd`·PATH 항목 0 · 서버 쪽 잡 로그 `[rcm] required tool fvm: missing` | `cd …-worker/cwd && RCM_WORKER_TOKEN=$W $RCM worker --server $S --pool linux --lanes 1 --data …-worker/data --once` |
| S1.5 | **통과** | #1~#4 의 `GET /jobs/<id>` · `/api/status` · `GET /jobs/<id>/log` · `server.log` · 워커 로그를 한 폴더에 모아 `grep -c`: `srvcwd` **0** · `tools` **1**(잡 #3 로그의 판정 줄 `[rcm] required tools: fvm ok` 의 단어 — PATH 항목 아님) · 서버 셸 `$PATH` 의 33개 항목을 하나씩 `grep -cF` → 전부 **0** · 잡 문서 넷에서 `/` 가 든 문자열은 `http://127.0.0.1:8811/#/jobs/N` 뿐 · 잡 로그 넷에 `/` 0줄 · `server.log` 의 `/Users` 는 기동 배너 `data …` 한 줄 | `for c in $(echo $PATH \| tr : '\n'); do cat * \| grep -cF -- "$c"; done` |
| S1.6 | **통과** | `pytest tests/test_requires.py tests/test_requires_m5l.py tests/test_requires_check.py -q` → 56 passed · `mutcheck --only requires-path-server-cwd` → caught(control green, mutant red) · `--only requires-cancel-ignored` caught · `--only requires-check-skipped` caught. 「서버 cwd ≠ 워크스페이스 + 상대 PATH」케이스: `tests/test_requires_m5l.py::test_a_tool_only_in_the_servers_cwd_is_missing_for_the_job` · `…is_found_through_a_relative_path_entry` · `test_an_empty_path_entry_means_the_workspace[3]` · `test_a_dot_path_entry_means_the_workspace` | 상대 PATH 변이 이름 `requires-path-server-cwd`(㉘) |
| S2.1 | **통과** | 넷 다 exit **2** · 서버 안 뜸(데이터 디렉터리도 안 생김 · 8811 의 기존 서버 `health ok` 그대로): `rcm: config: preset 'p' requires: an absolute path must end in a tool name, got '/opt/private-sdk/'` · `… got '/'` · `… got '/opt/private-sdk/.'` · `… got '/opt/private-sdk/..'` | 별도 설정 넷 `$RCM serve --config c1..4.toml` |
| S2.2 | **통과** | `needs-missing-abs`(`/usr/local/rcm-verify-missing-9f3a`) #5: exit 1 · `{"state":"failed","summary_code":"tool_missing","summary_args":{"tool":"rcm-verify-missing-9f3a"},"summary":"required tool rcm-verify-missing-9f3a is missing"}` — args 키 하나 · `GET /jobs/5 \| jq -c . \| grep -c /usr/local` → **0** · `/api/status`(`pools[0].recent[]` 의 #5 행 · 문서 전체) `grep -c /usr/local` → **0** · 잡 로그 `[rcm] required tool rcm-verify-missing-9f3a: missing` | `recent` 는 최상위가 아니라 `pools[].recent` 아래다 |
| S2.3 | **통과** | curl claim 한 #7(`needs-missing-remote`): finish `{"tool":"/opt/bin/"}` → **400** `{"error":"summary_args.tool must be a short tool name"}` · 이어 `{"tool":"fvm"}` → **200** `{"job_id":7,"state":"failed"}` · `GET /jobs/7` = `tool_missing` · `{"tool":"fvm"}` · `failed_step/last_step null` · `failures []`. claim 응답 `preset.requires == ["rcm-verify-no-such-tool-9f3a"]`, `preset` 키 `argv env env_passthrough name repo requires source_modes timeout_seconds` | `POST /worker/claim {"lane":1,"wait_seconds":0}` → `POST /worker/jobs/7/finish` |
| S2.4 | **통과** | `pytest -k "basename or slash or root or tool_name"` → 9 passed. `tests/test_requires_m5l.py::test_requires_rejects_an_absolute_path_without_a_tool_name[/dir/ · / · /opt/bin/ · /opt/. · /opt/..]` 가 `ConfigError` 를 단언 | |
| S3.1 | **통과** (현재 정책 = basename · 오너 결정 대기) | `needs-abs-true` #6: exit 0 · `succeeded` · `sentinel-6` 있음 · 잡 로그 `[rcm] required tools: true ok` · `grep -c '/usr/bin/true' log6` → **0**. 문서: `docs/configuration.md` 「an entry declared as `/opt/homebrew/bin/gh` shows up everywhere as `gh`」 · CHANGELOG 「an entry declared as an absolute path is named by its basename in the job log too」 — 코드와 문서가 같은 쪽(basename). G4.5 비고의 「선언한 그대로」는 이 커밋으로 없어졌다 | `$RCM run needs-abs-true … --dir treeD --poll` |
| S3.2 | **오류 → 수정(`31fbf5e`)** | 수정 전 `rcm check --config … --server $S --token $A`(exit 1) 의 행: `FAIL  local preset tools … · needs-abs-true (/usr/bin/true ok) · needs-missing-abs (/usr/local/rcm-verify-missing-9f3a missing) — checked in this shell, not the service's environment` — 잡 로그는 basename 인데 check 행은 선언한 경로 그대로(같은 브랜치 안에서 정책이 둘). `tests/test_requires_check.py` 도 `gate (sh ok · /bin/sh ok)` 를 정답으로 잠그고 있었다(리뷰 #109 C). 수정 뒤 실기: `… · needs-abs-true (true ok) · needs-missing-abs (rcm-verify-missing-9f3a missing) — …` · 행의 `—` 앞에 `/` **0** · PATH 값 없음 · exit 1(FAIL 정책은 G4.10 그대로) | `_local_preset_tools_row` 가 `runner.public_tool_name` 을 쓴다 |
| S4.1 | **통과** | 300 MB 한 파일(`treeBig`)은 자재화가 1초 미만이라(#8·#9 `started_at == finished_at`, 취소가 `job already finished (failed)`) 창을 못 맞췄다 → 2 KB × 60,000 파일 `treeMany` 로 재시도: #10 `/api/status` 의 `progress.phase == "materializing"` 첫 표본에서 `rcm cancel 10` → `{"job_id":10,"state":"cancelling"}` · 종료 뒤 `rcm wait --job 10` exit **2** · `{"state":"cancelled","summary_code":"cancelled_by","summary_args":{"by":"alice"},"exit_code":null,"summary":"cancelled by alice","failed_step":null,"last_step":null}` · transitions `uploading queued running cancelling cancelled` | `$RCM run needs-missing … --dir treeMany --no-wait` → 폴링 → `$RCM cancel 10 --server $S --token $A` |
| S4.2 | **통과** | `tests/test_requires_m5l.py::test_a_job_cancelled_while_materialising_ends_cancelled_not_tool_missing`(`Worker._materialize` 를 `threading.Event` 로 막고 `store.request_cancel` → set) → `cancelled` · `cancelled_by` · `exit_code None` · 판정 줄·STARTED 없음 · `…::test_a_cancel_that_lands_between_the_check_and_the_finish_still_wins`(검사 뒤·finish 전 취소) → `cancelled`. `Store.finish(…, only_from=(RUNNING,))` 가 다른 상태 행에 `False` 를 돌려주는 직접 단언은 `tests/test_store.py::test_finish_only_from_guards_races` | |
| S4.3 | **통과** | 종료 요청(`should_stop`)이면 runner 가 판정 줄 없이 `RunResult(lost=True)` 를 돌려주고 로컬 워커는 여느 유실처럼 **`lost`**(`worker_stopped_while_running`)로 닫는다 — `tests/test_requires_m5l.py::test_a_stop_seen_before_the_preflight_returns_lost` · 원격은 `…::test_the_remote_worker_reports_lost_when_stopping_before_the_preflight`(finish `outcome == lost` · `summary_code` 없음). 재큐가 아니라 `lost` 다(워커 재시작 계약 그대로) | |
| S4.4 | **통과** | 워커(`--once`)를 먼저 띄우고 `needs-missing-remote --dir treeMany --no-wait` #11 → `materializing` 첫 표본에서 `rcm cancel 11` → `cancelling` · 워커 stdout `lane 1: claimed #11 needs-missing-remote` → `lane 1: #11 cancelled in 19s`(워커가 취소를 보고 `cancelled` 로 finish — `failed` 를 보내지 않았다) · `GET /jobs/11` = `cancelled` · `cancelled_by {"by":"alice"}` · 라벨 null · `rcm wait` exit 2 · 워커 데이터에 파수꾼 없음 · 대장 0 | 서버 쪽 방어(`cancelling` 에 온 `tool_missing` → `cancelled`)는 `…::test_the_server_closes_a_cancelling_job_as_cancelled_over_a_tool_missing_finish` 가 잠근다 |
| S4.5 | **통과** | `sqlite3 <data_dir>/rcm.sqlite3 "SELECT count(*) FROM job_failures WHERE job_id=10"` → **0** · `GET /jobs/10` `failed_step null · last_step null · failures null` · 잡 #10 로그 **빈 파일**(판정 줄도 없다 — 검사 전에 취소를 봤다) | |
| LX.3 (8811) | **통과** (비고) | `/api/status` · #1~#11 의 `GET /jobs/<id>` · 잡 로그 전부 · `rcm jobs --json` · `server.log` · `run/cancel/check/worker` 의 stderr 를 모아 `grep -rn "$HOME\|/Users/\|/opt/homebrew"`: 기동 배너 `data <HOME>/.local/share/rcm-verify-m5l-s109` 한 줄 · `rcm check` 의 `local data dir … from <config>` 한 줄 · 그리고 `/api/status` 의 **`pools[0].hosts[0].disk.path`**(`<HOME>/.local/share/rcm-verify-m5l-s109`) — 마지막 것은 **L8**(`fix/status-disk-path-not-public` · 오너 결정 ①)의 것이지 이 PR 의 것이 아니다. 잡 문서·잡 로그·`rcm jobs --json` 은 0 | `<data_dir>/logs/` 는 없었다(서버 로그는 `server.log` 리다이렉트) |
| LX.5 (8811) | **통과** | `curl …/api/status \| jq .schema_version` → **1** · `SCHEMA_VERSION = 1`(`src/remote_ci_monitor/__init__.py`) · 이 PR 은 키 추가(`requires` in claim · `summary_code/args` in finish)만 | |

## G4 재확인 (S1~S4 가 바꾼 행)

| G4 행 | 여기서 | 결과 |
|---|---|---|
| G4.2 (경로 노출 0) | S1.5 · S2.2 | 통과 — 잡 문서·status·로그에 PATH 항목·절대경로 0 |
| G4.3 (라벨·대장 없음) | S1.4 · S2.3 · S4.5 | 통과 — `failed_step/last_step null` · `failures []` · 대장 0 |
| G4.5 (절대경로 선언의 로그 줄) | S3.1 | 이제 `[rcm] required tools: true ok`(basename) — G4 보고서의 「선언 문자열 그대로」비고는 이 커밋으로 바뀌었다 |
| G4.10 (`local preset tools` 행) | S3.2 | **오류 → 수정** — 행이 basename 을 찍는다 · FAIL/exit 1 정책은 그대로 |
| G4.13 (claim 의 `requires`) | S2.3 | 통과 — 옛 키 일곱 + `requires` |
| G4.14 · G4.15① (원격 finish 의 이름) | S2.3 | 통과 — `/opt/bin/` 400 · `fvm` 200 |
| G4.16 (원격 프리플라이트) | S1.4 | 통과 — 파수꾼 없음 · PATH 없음 |
| G4.18 (자동) | S1.6 | 통과 — 56 passed · 변이 ㉗㉘㉙ caught |

## 오류와 수정

| # | 항목 | 무엇이 | 수정 | 테스트(수정 전 빨강) |
|---|---|---|---|---|
| 1 | S3.2 (G4.10) | `rcm check --config` 의 `local preset tools` 행이 절대경로로 선언한 항목을 선언 문자열 그대로 찍었다(`needs-abs-true (/usr/bin/true ok)`). 잡 로그·요약·문서는 basename 인데 이 행만 경로라 코드와 문서가 서로 다른 말을 했다 — 시나리오 S3 「한쪽만 참이어야 한다」 | `31fbf5e` `fix(cli): check 의 local preset tools 행도 절대경로 대신 basename — 잡 로그와 같은 정책` — `cli._local_preset_tools_row` 가 `runner.public_tool_name` 으로 이름을 그린다 · `docs/configuration.md` 의 check 행 문단에 「The row names each entry the way the job log does — by its basename」 | `tests/test_requires_check.py::test_check_row_local_preset_tools_is_ok_when_everything_is_found`(`/usr/bin/true` → `true ok`, 행에 `/usr/bin` 없음) · `…::test_check_row_local_preset_tools_names_a_missing_absolute_declaration_by_basename`(`/opt/private-toolchains/fvm` → `fvm missing`, 행의 `—` 앞에 `/` 없음). 옛 단언 `gate (sh ok · /bin/sh ok)` 는 리뷰 #109 C 가 지적한 대로 바꿨다 |

검사(수정 뒤, 워크트리 `.venv`): `ruff check . && ruff format --check .` 통과 · `pytest` 전체 rc 0(실패 0) ·
`python scripts/mutcheck.py` **29/29 caught**(= `grep -c "Mutant(" scripts/mutcheck.py` = AGENTS.md·CONTRIBUTING.md 의
「29 known mutations」).

## 확인 못 함

없음. S4.1·S4.4 의 실기 창은 첫 시도(300 MB 한 파일)에서 못 맞췄지만 60,000 파일 트리로 맞췄다 — 두 행 다 실측이다.

## 오너 결정

- **S3(②)** — 현재 브랜치의 동작: 절대경로로 선언한 항목은 **잡 로그에서도 basename**(`[rcm] required tools: true ok`),
  `rcm check` 행도 basename(`31fbf5e` 뒤), 문서(`configuration.md` · CHANGELOG)도 basename. 즉 「로그도 basename」쪽으로
  코드·문서가 일치한다. 오너가 「유지(선언한 그대로)」를 고르면 `runner._preflight_lines` · `cli._local_preset_tools_row`
  · `tests/test_requires_m5l.py` 의 S3 세 건 · `tests/test_requires_check.py` 두 건 · `configuration.md` 「shows up
  everywhere as `gh`」 · CHANGELOG 「in the job log too」를 함께 되돌려야 한다 — 어느 쪽이든 check 행과 잡 로그는
  같아야 한다는 것이 이 보고의 수정이다.
- **L8(①)** — LX.3 에서 본 `/api/status` 의 `hosts[].disk.path` 는 이 PR 밖(실측만 적었다).

## 뒷정리

8811 서버 종료 · `~/.local/share/rcm-verify-m5l-s109{,-worker,.toml}` · 스크래치 트리 삭제 · 워크트리
`remote_ci_monitor-verify-m5l-s109` 와 로컬 브랜치 `docs/acceptance-m5l-verify-s109` 제거. PR #109 는 머지하지 않았다.
