# M5j 검증 보고 — G1 `step_timeline` · 2026-09-10 · PR #108 (`feat/jobs-step-timeline-for-finished` @ 632e84c)

- 대상: `docs/acceptance/m5j-verification-scenarios.md` §1 G1 (G1.1~G1.15) · GX.3(8802) · 변이 수. 명세는
  `docs/gate-optimization-workplan.md` §2 G1 · 결정 84, Codex 리뷰 G1 충돌 1~4.
- 환경: macOS 25.5 · Python 3.11.15 · 워크트리 `remote_ci_monitor-verify-m5j-g1`(브랜치
  `docs/acceptance-m5j-verify-g1`, PR 브랜치에서 분기) + 자기 `.venv`. 시험 서버 `rcm serve --config
  ~/.local/share/rcm-verify-g1.toml`: `127.0.0.1:8802` · `data_dir ~/.local/share/rcm-verify-g1` ·
  `advertise = false` · `lanes = 1` · `[notify]` 없음 · 시나리오의 프리셋 여섯 그대로. 토큰 alice(`cYts…`, 일반) ·
  root(`FToy…`, admin). 제출은 파일 하나짜리 디렉터리(`~/.local/share/rcm-verify-g1-submit/hello.txt`)를
  `--dir` 로. 운영 체크아웃·`~/.config/rcm`·`~/.local/share/rcm` 은 건드리지 않았다(아래 「실수 한 건」).
- 판정 기준: §0 — 명령을 실제로 돌려 본 값. 기대와 다르면 오류.
- 결과: **통과 15 / 15 · 오류 0 · 확인 못 함 0**. 시나리오 저자가 깨질 자리로 짚은 G1.5(DB 오류 → `null` + 코드) ·
  G1.8(v16 이 옮긴 라벨을 되살리지 않는다) · G1.11(취소 중 스텝 `ok: null`) · G1.13(마커 상한) · G1.9(wait/run
  JSON) · G1.10(`/api/status` 불변) 모두 명세대로였다. 코드 수정 없음. **테스트 한 개를 더했다**(G1.5 의 `rcm wait`
  종료 코드 조건이 기존 테스트에 없어서 — 커밋 1d5406b, 지금 구현으로 초록). 전제가 시나리오와 다른 행 둘(G1.4 ·
  G1.15)은 비고에 적었다 — 둘 다 PR #108 의 계약 자체는 지켜졌다.
- 실수 한 건: 첫 `rcm check --server … --token …` 에 `--config` 를 안 붙여 마지막 행이 운영 설정
  (`~/.config/rcm/server.toml`)의 data dir 을 **읽어** 보고했다(`rcm check` 는 읽기 검사, 수정·마이그레이션 없음).
  이후 명령은 전부 시험 설정만 썼다.

## G1 — 종료 잡의 `step_timeline`

| ID | 판정 | 실측 | 명령 · 비고 |
|---|---|---|---|
| G1.1 | 통과 | 잡 #2(`steps3`). 0.3초 표본 13개, 마지막 표본의 `progress.steps`: `a done ok 2.0 (09:14:54→56)` · `b done ok 1.0 (56→57)` · `c running ok null 1.0`. 종료 문서: `state succeeded` · `finished_at 09:14:58Z` · `job_seconds 4.0` · `progress` 키 없음 · `step_timeline = {timing as_received, steps_total 3, steps_total_partial false, steps 3}`. 닫힌 `a`·`b` 의 `{index,name,started_at,ended_at,seconds,ok}` 가 활성 표본과 **바이트 단위로 같다**(`cmp` SAME). `c.ended_at == finished_at` true · 셋 다 `ok true` | `rcm run steps3 --no-wait --no-join` → `curl /api/status \| jq '.pools[0].queue[]\|select(.id==2)\|.progress'` 반복 → `curl /jobs/2`. 활성 표본에만 있는 키: `phase current_index current_name current_seconds job_seconds job_started_at last_output_at failed_step last_step steps_done`; 타임라인 키는 `steps steps_total steps_total_partial timing` 넷, 스텝 키 여섯 |
| G1.2 | 통과 | 잡 #3(`nomarkers`). `step_timeline == {"timing":"as_received","steps_total":null,"steps_total_partial":true,"steps":[]}` · `step_timeline_error_code` 키 없음 · `failed_step`·`last_step` null | `rcm run nomarkers --poll` exit 0 → `GET /jobs/3` |
| G1.3 | 통과 | 잡 #4(`sleepy`). pause 뒤 제출 → `state queued · reason paused · started_at null` → cancel `{"job_id":4,"state":"cancelled"}` → resume. 문서: `state cancelled` · `started_at null` · `finished_at 09:15:18Z` · `step_timeline` 은 G1.2 와 같은 빈 객체(null 아님) · error_code 키 없음 · `failed_step`·`last_step` null | `rcm pause --token $R` → `rcm run sleepy --no-wait` → `rcm cancel 4` → `rcm resume` |
| G1.4 | 통과(전제 다름) | 잡 #5. `POST /jobs`(preset nomarkers, source.tree_hash 임의 64hex) → 201 `upload: /jobs/5/tree`. `PUT …/tree --data-binary 'garbage'` → **200** `{"job_id":5,"state":"queued","bytes":7}`(서버는 본문을 바이트로 흘려 받을 뿐 gzip 을 검사하지 않는다 — PR 이전부터). 워커 준비 단계에서 `failed` · `summary "snapshot rejected: not a valid tar.gz"` · `exit_code null` · **`started_at 09:16:20Z`**(claim 시각). `step_timeline` 은 빈 객체(null 아님) · error_code 없음 · `failures []`. `/api/status.recent` 에도 같은 행 | 시나리오의 「업로드 거절 4xx · `started_at null`」은 현 서버 동작과 다르다(거절은 실행 단계에서 난다). G1 의 계약 — 잡이 조용히 사라지지 않고, 마커 없는 종료 잡은 빈 타임라인 — 은 지켜졌다 |
| G1.5 | 통과 | `tests/test_step_timeline.py::test_a_store_error_reading_markers_is_null_plus_a_code`: `Store.markers` → `OperationalError("database is locked")` 일 때 **200** · `step_timeline null` · `step_timeline_error_code == "database_unavailable"`(경로·SQL·예외 문장 없음) · `state failed`·`last_step "a"`·`failures []` 그대로. **더한 테스트** `test_rcm_wait_exit_code_follows_the_state_when_the_timeline_cannot_be_read`: 진짜 워커로 `bad`(exit 2) 실행 → `markers` 를 `OperationalError("disk I/O error")` 로 바꾼 뒤 `rcm wait --job` → **종료 코드 1**(3 아님) · JSON `wait_exit_code 1` · `step_timeline null` + 코드 · `last_step "t"`. `mutcheck --only step-timeline-db-error-empty` → control green, mutant red | `pytest tests/test_step_timeline.py -q` 18 passed. wait 의 코드는 `wait_for_job` 이 상태로 정하고 타임라인은 안 본다(`cli.py:666`) |
| G1.6 | 통과 | `midfail` 두 번(#6, #7). #7: `state failed · exit_code 1 · failed_step "lint" · last_step "package"` — `/api/status.recent` 의 #7 행도 `lint`/`package` 로 **같다**. `step_timeline.steps`: `lint ok false 1.0` · `test ok false 1.0` · `package ok null 1.0`. `failed_step_guessed` 없음 · 타임라인 객체 안에 `failed_step` 없음 · 최상위 키 27개 중 타임라인이 더한 것은 `step_timeline` 하나 | `rcm run midfail --poll` ×2 → `GET /jobs/7` · `curl /api/status \| jq '.pools[0].recent[]\|select(.id==7)'` |
| G1.7 | 통과 | #7 `failures`: `[{"name":"lint","step":true},{"name":"test","step":true}]` — 가운데 스텝 `test` 가 `step true` · 순서 `lint` 먼저 · `failures_truncated false` | G1.6 의 문서 |
| G1.8 | 통과 | 서버 정지(pid 38064) → `sqlite3`: 전 `7\|failed\|lint\|package`, 대장 2행, `user_version 16` → UPDATE/DELETE/`PRAGMA user_version=15` → 후 `7\|failed\|build web\|`, 대장 0, v15. 재기동 → `backup/rcm.sqlite3.v15.bak`(180,224 B) 생성 · `user_version 16`(이 브랜치의 `DB_VERSION`; G5 전이라 17 아님) · 행 `7\|failed\|\|build web`. `GET /jobs/7`: **`failed_step null` · `last_step "build web"`** · `step_timeline.steps` = `lint false · test false · package null`(fail 마커가 남아 있으니 타임라인은 「lint 가 깨졌다」고 계산) · `failures []` · `failures_truncated false` — 재구성 라벨이 최상위로 되살아나지 않는다 | `kill <pid>` → `sqlite3 rcm.sqlite3 "UPDATE jobs SET failed_step='build web', last_step=NULL WHERE id=7; DELETE FROM job_failures WHERE job_id=7; PRAGMA user_version=15;"` → `nohup rcm serve --config …` |
| G1.9 | 통과 | 잡 #8(`steps3`). `rcm run … --poll` 마지막 stdout JSON(`wait_exit_code 0`) · `rcm wait --job 8`(exit 0, stdout 1줄) · `GET /jobs/8` — 셋의 `jq -S .step_timeline` 이 **바이트 단위로 같다**(`a 2.0 · b 1.0 · c 1.0`, 전부 ok true, steps_total 3). 문서: `docs/configuration.md` 「마커」 절 「`GET /jobs/<id>` (and so the JSON `rcm run` and `rcm wait` print) carries `step_timeline`」 · `docs/usage.md`·`usage.ko.md` §6 「The JSON」 · CHANGELOG 「this is now part of the contract」 | `cmp` ×2 → ALL_THREE_SAME |
| G1.10 | 통과 | 잡 #9(`sleepy`) 실행 중: `/api/status` → `schema_version 1` · 큐 행 `state running · progress 있음 · progress.steps[0] {name a, state running}` · `step_timeline` 키 없음. `GET /jobs/9` → `progress` 있음 · `step_timeline` 키 없음 · error_code 없음 | 제출 1.5초 뒤 `curl /api/status` · `curl /jobs/9` |
| G1.11 | 통과 | #9 를 약 3초 뒤 `rcm cancel 9 --token $A` → `cancelling` → 문서 `state cancelled · exit_code -15 · cancelled_by alice · started_at 09:17:40Z · finished_at 09:17:44Z` · `failed_step null · last_step null` · 마지막 스텝 `{index 1, name a, ended_at 09:17:44Z, seconds 4.0, ok null}` · `ended_at == finished_at` true | `seconds 4.0`: 취소 명령이 3초 시점, 종료(SIGTERM 뒤 `cancelled`)가 4초 시점 — ±1 안. `ok` 는 False 로 물들지 않았다 |
| G1.12 | 통과 | 잡 #10(`slow`, timeout 5). `rcm run slow --poll` JSON `wait_exit_code 2` → 문서 `state timed_out · exit_code -15 · last_step "a" · failed_step null` · 마지막 스텝 `{name a, ended_at 09:17:49Z, seconds 5.0, ok null}` · `ended_at == finished_at` true | |
| G1.13 | 통과 | 잡 #11(`many`). `events` 표 `kind='marker'` **450** 행(`state` 4). 문서: `step_timeline.steps` 길이 **300**(`s1`…`s300`) · `steps_total 300` · `steps_total_partial true` · 타임라인 키 넷뿐(절단 키 없음 = 절단 없음) · `failures` **100** · `failures_truncated true` · `last_step "s300"` · `failed_step null`. `curl` 응답 `real 0.00`(문서 50,742 B). `rcm wait --job 11` exit 1 · stdout 1줄에 스텝 300 | `sqlite3 … "SELECT kind,count(*) FROM events WHERE job_id=11 GROUP BY kind"` |
| G1.14 | 통과 | `pytest tests/test_step_timeline.py tests/test_progress.py tests/test_docs_step_timeline.py -q` 33 passed(추가 테스트 뒤 관련 파일 18 passed) · `python scripts/mutcheck.py --only step-timeline-db-error-empty` → `[OK ] control green, mutant red (exit 1, 0.6s)` · `ruff check .` / `ruff format --check .` 통과 | |
| G1.15 | 통과(비고) | `PLAN.md` 「서버 API」 `GET /jobs/{id}` 행(239행)에 모양 전부 — 객체 · 키 여섯 · 마커 없으면 `steps: []` · 못 읽으면 `null` + `step_timeline_error_code`(fail-open 금지) · 재구성 `failed_step` 안 실음 · `rcm wait`·`rcm run` JSON 은 이 문서의 복사(계약). 결정 84 는 `docs/gate-optimization-workplan.md` §4 표(153행)에 있고 PLAN 행이 그것을 가리킨다 | PLAN.md 결정 표는 83까지다 — M5i 도 결정 73~83 을 별도 docs 커밋(d614362)으로 한 번에 넣었으니 관행대로이고, 「PLAN 결정 84~87 이 표에」는 GX.6(verify-g3-docs)의 몫. 시나리오가 이름 댄 「진행 — 스텝 마커 프로토콜」·「스키마 v1」 절 자체에는 `step_timeline` 문구가 없다(그 절의 규칙 — 종료 코드로 실패 스텝을 고르지 않는다 · queued 엔 진행 칸 없음 — 과 어긋나지는 않는다) |

## 가로지르기 (G1 몫)

| ID | 판정 | 실측 | 비고 |
|---|---|---|---|
| GX.3 (8802) | 통과 | `/api/status.schema_version == 1`. v0.2.6 릴리스 커밋(73584b5)의 `SCHEMA_VERSION = 1` 과 같다. `git diff 73584b5 HEAD -- src/remote_ci_monitor/core/status.py` → **33줄 추가 · 0줄 삭제**(`step_timeline_json` 함수 하나) — 옛 키 제거·의미 변경 없음. 종료 문서 최상위 키 27개 중 새 키는 `step_timeline`(오류 때 `step_timeline_error_code`) | origin 에 `v0.2.6` 태그가 아직 없다(자동 태그 미발동, `v0.2.4` 까지) — 릴리스 커밋으로 대신 비교 |
| GX.2 (G1 항목) | 통과 | CHANGELOG `[Unreleased]` Added 「A finished job keeps its step times.」 — `rcm run`·`rcm wait` JSON 계약 · `steps: []` 대 `null` + 코드 · `failed_step`·`last_step` 불변 · `failures[].step` · 「`schema_version` stays 1 — a key was added」 · 링크 `#108` | |
| 변이 수 | 통과 | `scripts/mutcheck.py` `Mutant(` 27개 · AGENTS.md:72 「27 known mutations」 · CONTRIBUTING.md:13 「27 known mutations」 | G4 PR 과 합치면 양쪽 더해 28(PR 설명대로) |
| 로그 | 통과 | `server.log` 6줄(기동 2회) — `error`/`traceback` 0건 · `$HOME` 이 든 줄은 기동 배너의 `data …` 2줄뿐(기동당 1줄) | GX.4 는 verify-g5 몫이라 참고만 |

## 오류와 수정

없음. 코드는 손대지 않았다.

- 커밋 1d5406b `test(server): 마커 조회가 깨져도 rcm wait 종료 코드는 상태대로 — G1.5 잠금` — 검증 근거로 남긴
  테스트(진짜 워커 + 실제 `rcm wait`). 지금 구현으로 초록.

## 확인 못 함

없음.

## 남긴 것 (오너 대기 아님 · 참고)

- G1.4 의 전제: `PUT /jobs/<id>/tree` 는 gzip 을 검사하지 않고 200 을 준 뒤 워커의 준비 단계에서 `snapshot rejected`
  로 실패시킨다(`started_at` 이 claim 시각으로 찍힌다). 시나리오 문구(4xx · `started_at null`)를 이 동작에 맞추거나,
  「업로드 시점 거절」을 별도 요청으로 다룰지는 M5j 밖이다.
- 「시작 전 실패 → `started_at null` + 빈 타임라인」 자체는 G1.3(취소)과 자동 테스트
  `test_a_job_that_failed_before_it_started_has_an_empty_timeline`(store.finish 로 만든 `started_at null` 실패 잡)로 확인했다.

## 정리

서버 정지 · `~/.local/share/rcm-verify-g1`(data_dir · 백업 포함) · `rcm-verify-g1.toml` · 제출 디렉터리 삭제 · 포트 8802 해제
확인 · 워크트리 `remote_ci_monitor-verify-m5j-g1` 제거.
