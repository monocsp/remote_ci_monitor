# M5l 검증 보고 — S11 `step_timeline` 의 CHANGELOG 위치 · 예외 폭 · 시계 폴백 · 2026-09-13 · PR #108 (`feat/jobs-step-timeline-for-finished` @ bb84070 → 수정 9e495b4)

- 시나리오: `docs/acceptance/m5l-verification-scenarios.md` §1 S11(리뷰 `docs/reviews/2026-09-13-impl-reviews/pr-108.md` D-1 · B-1 · B-2 · C-1~C-5 · D-3) + LX.5(8813). 실행 주체: 격리 에이전트(verify-s108, 중단된 세션을 이어받아 처음부터 다시 돌렸다).
- 워크트리 `../remote_ci_monitor-verify-m5l-s108`(`docs/acceptance-m5l-verify-s108`, 기준 `origin/feat/jobs-step-timeline-for-finished` = bb84070) + 자기 `.venv`(python3.11 · `pip install -e '.[dev]'`).
- 시험 서버 8813: `<HOME>/.local/share/rcm-verify-m5l-s108.toml`(`bind = "127.0.0.1"` · `advertise = false` · `lanes = 1` · `[notify]` 없음) · `data_dir = <HOME>/.local/share/rcm-verify-m5l-s108` · 프리셋 `ok` `sleepy` `steps3` `midfail`(§0 의 꼴 + S11 준비) · `./.venv/bin/rcm serve --config …` 를 `nohup` 으로. 토큰 `alice`(`WjSW…`). 끝나고 서버를 내리고 `data_dir` 과 설정을 지웠다(8813 해제 확인).
- S12.2 의 실기(`lanes = 2`)도 이 서버에서 했다 — 보고는 `2026-09-13-m5l-verify-s106.md`. 그 실측이 #108 의 문서 문장 하나를 고치게 했다(아래 「S12.2 파급」).

## 결과 요약

| ID | 판정 | 실측 |
|---|---|---|
| S11.1 | **오류 → 수정 9e495b4** | `awk` → `entry 11` · `release 25`: 항목은 `[Unreleased]` 안에 있다. 그러나 항목 문단을 `## [0.2.6]` 의 `### Added` 아래로 옮기고 `pytest tests/test_docs_step_timeline.py` → **5 passed**(빨갛지 않다 — 리뷰 C-1 그대로, `unreleased()` 가 `[0.1.0]` 앞까지 전부를 돌려준다). 수정 뒤 같은 이동 → `FAILED …::test_the_changelog_entry_sits_above_the_first_release_heading` · 되돌리면 초록 |
| S11.2 | 통과 | `steps3` 잡 #1 종료 뒤 `UPDATE events SET payload='x' WHERE id=(SELECT min(id) …)` → `GET /jobs/1` **200** · `step_timeline: null` · `step_timeline_error_code: "internal_error"` · `state succeeded` · `summary null` · `failed_step null` · `last_step null` · `artifacts.state disabled` 그대로 · `rcm wait --job 1` exit **0**, 마지막 JSON 줄에 같은 `null` + `internal_error` · 서버 로그에 traceback 0 |
| S11.3 | 통과 | `midfail` 잡 #2 에 같은 손상 → 200 · `failures: [{name: "lint", step: true, seen: 1, window: 1, verdict: "unknown", …}]`(비어 있지 않다) · `failed_step: "lint"`(저장값) · `step_timeline: null` + `internal_error` |
| S11.4 | 통과 | 새 `steps3` 잡 #3 · `UPDATE jobs SET finished_at=NULL WHERE id=3` → `GET /jobs/3` 두 번(1초 간격): 둘 다 `{state: succeeded, finished_at: null, step_timeline: null, step_timeline_error_code: "no_finished_at"}` · `jq -S .` 전체 문서가 **같다** · 되돌린 뒤 `steps` 3개 · 코드 `null` |
| S11.5 | 통과 | `pytest tests/test_step_timeline.py -q` **16 passed** · `mutcheck --only step-timeline-db-error-empty` → `control green, mutant red (exit 1, 0.7s)` · C-2 짝 = `test_a_broken_marker_row_is_null_plus_a_code_not_a_500`(payload `'not json'` → `ValueError` 경로) · C-3 = `test_a_finished_job_without_finished_at_is_unknown_not_a_moving_timeline` |
| S11.6 | **오류 → 수정 9e495b4** | `docs/configuration.md:64` 「the last step of a failed or cancelled job is `null`」— `::rcm::fail::<name>`·`::rcm::step-end::fail` 로 선언된 스텝은 `false` 다(`progress.py` `fail_seen`, S11.3 의 잡 #2 실측 `ok: false`). 「… is `null` unless the script declared it failed (…), in which case it is `false`」로 고치고 `test_configuration_says_a_finished_job_keeps_its_step_times` 가 그 문구를 잠근다(수정 전 빨강) |
| S11.7 | 기록 | `docs/acceptance/reports/2026-09-10-m5j-verify-g1.md:1` 「@ 632e84c」만 있고 0ffb845 와의 관계 줄은 없다 — 오류 아님(리뷰 D-2 P2). 이 보고가 대신한다: 632e84c 이후 커밋(1d5406b · 0ffb845 · fc29f20 · 3c2e2c1 · bb84070)은 테스트·문서·오류 경로 보완이며 S11.2~S11.5 가 그 뒤의 코드를 실기로 지났다 |
| LX.5(8813) | 통과 | `curl …/api/status \| jq .schema_version` → **1** |

오류 2(S11.1 · S11.6) + S12.2 파급 1 → 수정 **9e495b4**(`docs(m5l): step_timeline 문서 보완 — 절 위치 잠금 · ok 문장 · 자기 포함 수`). 확인 못 함 0.

### S12.2 파급 — `concurrent_at_start` 문장

S12.2(b) 실측(`lanes = 2`, `sleepy` 둘): 첫 잡 `concurrent_at_start: 1` · 둘째 `2` — 자기 포함이다(#106 2bc6e20 의 정의와 같다). #108 의 `docs/usage.md`·`docs/usage.ko.md` §6 와 CHANGELOG 는 「how many jobs were running when it started」만 말해 혼자 돈 잡을 `0` 으로 읽게 했다. 같은 커밋에서 「itself included — a job that ran alone reads `1`」(ko: 「자기 포함이라 혼자 돌았으면 `1`」)을 두 거울과 CHANGELOG 에 적고 `test_both_usage_guides_say_concurrent_at_start_counts_itself[en|ko]` 가 잠근다(수정 전 빨강).

## 수정 9e495b4 의 검사

- 빨간 테스트: 새 단언 셋(절 위치 · `ok` 문장 · 자기 포함)을 먼저 얹고 `pytest tests/test_docs_step_timeline.py` → **3 failed**(`…counts_itself[en]` · `[ko]` · `…keeps_its_step_times`); 문서 수정 뒤 **8 passed**. 절 위치 테스트는 HEAD 에서 초록이므로 항목 이동으로 빨강을 확인했다(위 S11.1).
- `ruff check . && ruff format --check .` 통과(한글 docstring 폭으로 E501 하나를 접었다) · 전체 `pytest`(3434 수집) exit 0 · `python scripts/mutcheck.py` **27/27 caught**.
- push 뒤 #108 CI run 34733331825: `secrets` · `smoke`(ubuntu · macos) · `unit`(ubuntu 3.11 · 3.13 chrome, macos 3.13) · `test` 전부 pass.

## 명령 그대로

```sh
# 준비
./.venv/bin/rcm serve --config <HOME>/.local/share/rcm-verify-m5l-s108.toml     # nohup … &
A=$(./.venv/bin/rcm token --config <그 파일> add alice | tail -1)
for i in 1 2 3; do mkdir -p <data_dir>/j$i; echo $i > <data_dir>/j$i/f; done
./.venv/bin/rcm run steps3  --server http://127.0.0.1:8813 --token $A --dir <data_dir>/j1 --poll   # #1 succeeded
./.venv/bin/rcm run midfail --server http://127.0.0.1:8813 --token $A --dir <data_dir>/j2 --poll   # #2 failed: lint
# S11.1
awk '/A finished job keeps its step times/{print "entry", NR} /^## \[0\./{print "release", NR; exit}' CHANGELOG.md
# (항목 문단을 [0.2.6] ### Added 아래로 옮긴 뒤) ./.venv/bin/pytest tests/test_docs_step_timeline.py -q ; 되돌림
# S11.2 / S11.3
/usr/bin/sqlite3 <data_dir>/rcm.sqlite3 "UPDATE events SET payload='x' WHERE id=(SELECT min(id) FROM events WHERE job_id=1 AND kind='marker')"
curl -s -w '\n%{http_code}' http://127.0.0.1:8813/jobs/1 | jq -c '{state, summary, failed_step, last_step, failures, step_timeline, step_timeline_error_code}'
./.venv/bin/rcm wait --job 1 --server http://127.0.0.1:8813 --token $A ; echo rc=$?
# S11.4 (잡 #3)
FIN=$(sqlite3 … "SELECT finished_at FROM jobs WHERE id=3"); sqlite3 … "UPDATE jobs SET finished_at=NULL WHERE id=3"
curl -s …/jobs/3 > r1.json; sleep 1; curl -s …/jobs/3 > r2.json; diff <(jq -S . r1.json) <(jq -S . r2.json) && echo IDENTICAL
sqlite3 … "UPDATE jobs SET finished_at='$FIN' WHERE id=3"
# S11.5
./.venv/bin/pytest tests/test_step_timeline.py -q ; ./.venv/bin/python scripts/mutcheck.py --only step-timeline-db-error-empty
# S11.6 / S11.7
grep -n "failed or cancelled\|unless the script declared" docs/configuration.md
grep -n "@ \|632e84c\|0ffb845" docs/acceptance/reports/2026-09-10-m5j-verify-g1.md
```

실측 출력(발췌):

```
S11.2  http=200 {"state":"succeeded","summary":null,"failed_step":null,"last_step":null,"artifacts_state":"disabled","step_timeline":null,"step_timeline_error_code":"internal_error"}
       wait rc=0 {"state":"succeeded","step_timeline":null,"step_timeline_error_code":"internal_error","wait_exit_code":0}
S11.3  http=200 {"state":"failed","failed_step":"lint","last_step":"lint","failures":[{"name":"lint","step":true,"seen":1,"window":1,"window_unnamed":0,"first_seen_job_id":2,"last_seen_job_id":2,"verdict":"unknown"}],"step_timeline":null,"step_timeline_error_code":"internal_error"}
S11.4  {"state":"succeeded","finished_at":null,"step_timeline":null,"step_timeline_error_code":"no_finished_at"}  whole doc IDENTICAL
       (복구 뒤) {"finished_at":"2026-09-13T02:17:20Z","steps":3,"step_timeline_error_code":null}
S11.5  16 passed · [OK ] step-timeline-db-error-empty: control green, mutant red (exit 1, 0.7s)
```

## 비고

- 첫 S11.4 시도는 잡 #1 로 했는데 S11.2 에서 깨 둔 마커 행이 먼저 잡혀 `internal_error` 가 나왔다 — 손상 없는 새 잡(#3)으로 다시 했다. 오류 코드의 우선순위(마커 읽기 실패 > `finished_at` 없음)는 코드 순서 그대로다.
- 손상된 마커 행이 있어도 서버 로그에 traceback 이 없다(`grep -ci "traceback\|error" server.log` → 0) — 500 이 아니라 코드로 내려앉는다는 뜻이다.
