# M5l 검증 보고 — L1 오프라인 dry-run 의 오류 경로 (`fix/gc-offline-error-paths` · PR #117)

- 날짜: 2026-09-13
- 검증한 커밋: `origin/fix/gc-offline-error-paths` **cc77b3c** (`docs(changelog): L1 항목의 PR 링크를 #117 로`;
  수정 본체 `f83e93a fix(gc): 오프라인 dry-run 의 오류 경로 — sqlite 오류·사본 잔류는 3, 복원 안내는 실제 백업`)
- 명세: `docs/acceptance/m5l-verification-scenarios.md` §0 · §1 L1 (L1.1 ~ L1.11) · 근거 리뷰 `docs/reviews/2026-09-13-impl-reviews/pr-87.md` B P1 셋 + P2
- 워크트리: `remote_ci_monitor-verify-m5l-l1` (`docs/acceptance-m5l-verify-l1`, 기준 cc77b3c) · 자기 `.venv` (Python 3.11) · `DB_VERSION` = **16** (아래 `V`)
- 설정: `<HOME>/.local/share/rcm-verify-m5l-l1.toml` (`bind 127.0.0.1` · `port 8814` · `advertise = false` · `[notify]` 없음 · 프리셋 `ok`) — 서버는 띄우지 않았다(L1 은 서버 없음). L1.5~L1.7 과 L1.4·L1.10(엄격) 은 둘째 `data_dir` `…/rcm-verify-m5l-l1b`(같은 꼴의 설정 `…-l1b.toml`). 끝나고 두 `data_dir` 과 설정을 지웠다.
- 도구: `/usr/bin/sqlite3` 3.51.0 (`ALTER TABLE … DROP COLUMN` 된다) · `chflags`. `env | grep -E 'RCM_|XDG_'` 는 비어 있었다.
- 옛 버전 픽스처: `./.venv/bin/rcm token --config … list` 로 한 번 열어 만든 뒤 `sqlite3 <data_dir>/rcm.sqlite3 "PRAGMA user_version=15"`.

## 판정표

| ID | 판정 | 실측 | 명령/비고 |
|---|---|---|---|
| L1.1 | **통과** | `rc=3` · stderr **1줄** `rcm: gc: the copy could not be migrated from schema v15 to v16: no such column: last_step — a new server would fail the same way on restart. Nothing was changed (exit 3: unknown).` · `Traceback` 0줄 · 원본 `PRAGMA user_version` **15** 그대로 · `ls $TMPDIR \| grep -c rcm-gc-dryrun` **0** (전·후 모두) | v15 픽스처에 `sqlite3 … "ALTER TABLE jobs DROP COLUMN last_step"` → `./.venv/bin/rcm gc --dry-run --config <설정>; echo rc=$?` |
| L1.2 | **통과** | `tests/test_offline_gc.py`: `test_a_raw_sqlite_error_on_the_copy_is_unknown_without_a_traceback`(`_MIGRATIONS[V]` 를 `UPDATE jobs SET no_such_column=1` 로 — 실제 `sqlite3.OperationalError`) · `test_a_sqlite_error_raised_by_migrate_itself_is_also_unknown`(`Store.migrate` → `sqlite3.IntegrityError`, `--json` 도 stdout 비어 있음) · `test_main_does_not_let_a_sqlite_error_escape_as_a_traceback`(`main()` 안전망). 리뷰 C 의 「`StoreError` 를 직접 던진다」 테스트(`test_a_migration_that_fails_on_the_copy_is_unknown_and_names_the_restart_risk`)는 남고 짝이 생겼다 | `pytest tests/test_offline_gc.py tests/test_store_m5i.py tests/test_cli_serve_refusal.py -q` → **30 passed** |
| L1.3 | **통과** | `test_a_scratch_copy_that_cannot_be_removed_is_reported_and_is_not_a_success`: `cli.shutil.rmtree` 를 `OSError(EBUSY)` 로 → `rc == 3` · stderr 에 `warning` + `copy` + 남은 디렉터리 이름 + `Resource busy` + `exit 3` · stdout 의 JSON 은 `offline.copy == true`(계획은 찍힌다) · 원본 `user_version` 불변. `mkdtemp` 실패(`ENOSPC`)도 `test_a_scratch_directory_that_cannot_be_made_is_unknown` 으로 3 | 자동 (위 pytest) |
| L1.4 | **통과(실기)** | `TMPDIR=<data_dir>/tmp` 로 dry-run 을 돌리며 곁에서 `find … -path '*rcm-gc-dryrun-*' -type f -exec chflags uchg {} +` 를 무한 반복 → 창을 맞췄다: `rc=3` · stderr 2줄 — `rcm: gc: the copy could not be migrated from schema v15 to v16: attempt to write a readonly database — … (exit 3: unknown).`(uchg 가 사본 마이그레이션보다 먼저 걸렸다) + `rcm: warning: the temporary copy of the database was left behind in <data_dir>/tmp/rcm-gc-dryrun-50gydorl: [Errno 1] Operation not permitted: 'rcm.sqlite3' — remove that directory yourself (exit 3: the copy was not cleaned up).` · stdout 없음 · 남은 디렉터리에 `rcm.sqlite3`·`-shm`·`-wal`(uchg)·`backup/` · 원본 `user_version` 15 | `chflags -R nouchg` 로 풀고 지웠다. 「지웠다」는 거짓 성공 없음 |
| L1.5 | **통과** | 셋 다 **`…/rcm-verify-m5l-l1b/backup/rcm.sqlite3.v15.bak`** 를 가리킨다(`v16.bak` 아님 — 그 파일은 없다): gc `rc=3` `rcm: gc: database schema version 17 is newer than this build (16) — stop the service, restore <HOME>/.local/share/rcm-verify-m5l-l1b/backup/rcm.sqlite3.v15.bak (and remove rcm.sqlite3-wal/-shm), or upgrade (exit 3: unknown)` · serve `rc=2` `rcm: cannot start server: database schema version 17 … restore …/backup/rcm.sqlite3.v15.bak …` · token `rc=2` `rcm: database schema version 17 … restore …/backup/rcm.sqlite3.v15.bak …` | 새 `data_dir` 에 DB 를 만들고 `PRAGMA user_version=17` · `mkdir backup && cp rcm.sqlite3 backup/rcm.sqlite3.v15.bak && sqlite3 backup/rcm.sqlite3.v15.bak "PRAGMA user_version=15"` → `rcm gc --dry-run --config …` · `rcm serve --config …` · `rcm token --config … list` |
| L1.6 | **통과** | `v14.bak` 을 더해도 셋 다 **`v15.bak`**(가장 높은 것)을 가리킨다 · rc 3/2/2 그대로 | `cp … backup/rcm.sqlite3.v14.bak; sqlite3 … "PRAGMA user_version=14"` 뒤 같은 셋 |
| L1.7 | **통과** | `backup/` 을 지우자 셋 다 `.bak` 이름을 만들지 않고 `… newer than this build (16) — no migration backup found in <HOME>/.local/share/rcm-verify-m5l-l1b/backup — stop the service, restore a copy of the database made before the upgrade (and remove rcm.sqlite3-wal/-shm), or upgrade` (gc 는 뒤에 `(exit 3: unknown)`) · rc 3/2/2. 문서: `docs/operating.md` 「Going back to the old build」 — 「The file the message names is the highest `rcm.sqlite3.v<n>.bak` that … and if there is none the message says `no migration backup found` and leaves you the copy you made in step 5」 · 「copy the file the message names over `rcm.sqlite3`」 | `rm -rf backup` 뒤 같은 셋 · `grep -n bak docs/operating.md` |
| L1.8 | **통과** | `rc=0` · `user_version` 15 → **16** · `backup/rcm.sqlite3.v15.bak` 생성 · stderr **1줄** `warning: could not list <HOME>/.local/share/rcm-verify-m5l-l1/backup to prune old backups: [Errno 13] Permission denied: '<HOME>/.local/share/rcm-verify-m5l-l1/backup'` · `Traceback` 0줄 · stdout 은 토큰 표(비어 있음) | v15 픽스처 · `mkdir -p backup && chmod 300 backup`(`d-wx------`) → `rcm token --config … list; echo rc=$?` → `chmod 700 backup; ls backup` |
| L1.9 | **통과** | `python scripts/mutcheck.py --only offline-gc-ignores-sqlite-error` → `[OK ] … control green, mutant red` · `grep -c "Mutant(" scripts/mutcheck.py` = **27** = AGENTS.md·CONTRIBUTING.md 「27 known mutations」 · 주석 번호: docstring ㉔ `offline-gc-opens-original` · ㉕ `v16-without-ledger-check` · ㉖ `client-wheel-any-name` · ㉗ `offline-gc-ignores-sqlite-error` 이고 구현부 주석도 ㉔·㉕·㉗·㉖ 로 이름과 맞는다(구현부에서 ㉗ 블록이 ㉖ 앞에 놓여 순서만 다르다 — 번호·이름은 맞으니 오류 아님, 비고) | 자동 |
| L1.10 | **통과(기록)** | `rc=0` · stdout 첫 줄 `note: planned on a temporary copy of <HOME>/.local/share/rcm-verify-m5l-l1/rcm.sqlite3 (schema v15 → v16); the database itself was not changed` · 원본 `user_version` **15** · 사본 정리됨(`rcm-gc-dryrun` 0) · `--json \| jq .offline` = `{"database":"<HOME>/.local/share/rcm-verify-m5l-l1/rcm.sqlite3","schema_version":15,"planned_with_schema":16,"copy":true}` — **`offline.database` 는 절대경로다**(§2 ③ 오너 결정 대기 · 실측만 적는다). `-wal`/`-shm`: 픽스처에 이미 있던 상태(운영 모양 — 서비스가 돌면 늘 있다)에서는 새로 생기지 않았다. 다만 체크포인트 뒤 `-wal`·`-shm` 을 지운 DB 로 다시 돌리면 **빈 `-wal`(0 B)과 `-shm` 이 생긴다** — WAL 모드 DB 를 `mode=ro` 로 여는 SQLite 자체의 동작(#118 의 테스트 주석 「읽기 전용 연결도 WAL DB 를 열면 `-wal` 을 만든다」와 같다). 내용·`user_version` 은 불변. `immutable=1` 로 피하면 WAL 에만 있는 변경을 놓치므로(기존 테스트가 잠근 것) 고칠 대상이 아니라 명세 문구의 조건(「이미 있으면」)으로 적는다 | `rcm gc --dry-run --config …; echo rc=$?` · `… --json 2>/dev/null \| jq -c .offline` · `ls -lT <data_dir>` |
| L1.11 | **통과** | CHANGELOG `[Unreleased]` `### Fixed` 첫 항목: 「exits 3 on every incomplete path」 · 「raw `sqlite3.Error` … now one line on stderr and exit 3」 · 「a copy that could not be removed afterwards, is exit 3 as well — the copy … is named on stderr instead of being reported as cleaned up」 · 「highest `rcm.sqlite3.v<n>.bak` actually present … `no migration backup found`」 · 「`backup/` directory that cannot be listed … warns」 · 링크 `[#117](…/pull/117)`. `docs/operating.md` 「Upgrade」 2단계: 「a `StoreError` or any SQLite error from a migration statement alike — the command exits 3」 · 「If the copy cannot be removed afterwards, the command also exits 3 and names the directory that was left behind」 | `awk '/^## \[Unreleased\]/,/^## \[0.2.6\]/' CHANGELOG.md` · `awk '/^## Upgrade/,/^### Going back/' docs/operating.md` |

## 오류와 수정

없음 — L1.1 ~ L1.11 전부 실제 모양의 입력으로 실제 경로를 지났다. 이 브랜치에 추가 커밋은 이 보고서뿐이다.

## 확인 못 함

없음(L1.4 선택 실기까지 창을 맞췄다).

## 비고

- L1.10 의 `offline.database` 절대경로는 §2 ③ 오너 결정 사항 — 이 보고는 실측만 적는다(리뷰 #87 B P2).
- L1.4 에서 `uchg` 가 사본에 먼저 걸리면 마이그레이션 실패 문장이 앞서고 정리 실패 경고가 뒤따른다(2줄). 두 경로가 한 실행에서 함께 보인 것이고 종료 코드는 3 — 명세의 「계획은 찍혀도 되지만 종료 코드는 3」과 어긋나지 않는다.
- `scripts/mutcheck.py` 의 ㉗ 변이 이름·번호는 PR #118(L5)도 ㉗ 을 쓴다(`serve-store-before-bind`, 역시 27). 둘 중 나중에 머지되는 쪽이 ㉘·28 로 맞춰야 한다(#118 본문이 이미 「머지 때 맞춘다」고 적었다).
- PR #117 CI(cc77b3c): `secrets` · `smoke (macos-latest)` · `smoke (ubuntu-latest)` · `test` · `unit (macos-latest, 3.13)` · `unit (ubuntu-latest, 3.11, google-chrome)` · `unit (ubuntu-latest, 3.13, google-chrome)` 전부 pass.
