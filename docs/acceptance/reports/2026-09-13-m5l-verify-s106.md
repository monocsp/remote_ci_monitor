# M5l 검증 보고 — S12 두 레인 문서: `flock` · `concurrent_at_start` · `step_timeline` 순서 · 2026-09-13 · PR #106 (`docs/admission-two-lane-experiment` @ 82924d5)

- 시나리오: `docs/acceptance/m5l-verification-scenarios.md` §1 S12(리뷰 `docs/reviews/2026-09-13-impl-reviews/pr-106.md` A · B-1~B-5 · C · D). 실행 주체: 격리 에이전트(verify-s106, 중단된 세션을 이어받아 처음부터 다시 돌렸다).
- 워크트리 `../remote_ci_monitor-verify-m5l-s106`(`docs/acceptance-m5l-verify-s106`, 기준 `origin/docs/admission-two-lane-experiment` = 82924d5) + 자기 `.venv`. 실기(S12.1 · S12.2)는 verify-s108 의 8813 서버와 `data_dir`(`<HOME>/.local/share/rcm-verify-m5l-s108`)을 썼다 — #106 은 코드가 없어 `concurrent_at_start` 를 주는 서버는 #108 브랜치의 것이다. 끝나고 서버·`data_dir`·`/tmp/gate-heavy.lock` 을 지웠다.
- 이 Mac: `which flock` → not found(리뷰 B-1 의 조건 그대로).

## 결과 요약

| ID | 판정 | 실측 |
|---|---|---|
| S12.1 | 통과 | 절의 `sh` 블록을 그대로 `<data_dir>/lock.sh` 로(`flutter test ...` → `sleep 3`): `bash lock.sh & sleep 0.3; time bash lock.sh; wait` → 첫 rc 0 · 둘째 rc 0 · 둘째 `real 5.743 s`(≥ 2.5 — 첫 번째가 락을 쥔 동안 기다렸다) · `command not found` 없음(`python3 -c 'import fcntl; fcntl.flock(9, fcntl.LOCK_EX)'`) · 두 줄 다 `\|\| exit 3` · 락 파일을 못 열게(`/nonexistent-dir/x.lock`) 하면 rc **3** 으로 멈춘다 |
| S12.2 | 통과 — (b) | `grep concurrent_at_start docs/configuration.md src/…/server.py src/…/core/status.py` → 이 브랜치엔 문서 472행뿐(코드는 #108 `server.py:1219` `doc["concurrent_at_start"] = self.store.concurrent_at_start(job.id)`). 8813 을 `lanes = 2` 로 재기동해 `sleepy` 둘(#6 · #7, 1초 간격): `GET /jobs/6` → `concurrent_at_start: 1` · `GET /jobs/7` → **2**(정수 · null 아님 — 자기 포함, 문서 472~475행 「itself included, so a job that ran alone reads `1`」와 일치. 시나리오의 「0 · 1」은 자기 포함 전 표기다) · `/api/status` 키 6개 전후 동일 · `schema_version 1` · status 문서 어디에도 `concurrent_at_start` 없음 · CHANGELOG 키 한 줄은 #108 항목(20행)에 있다. #108 의 `usage.md`·`usage.ko.md`·CHANGELOG 문장이 「돌던 잡 수」만 말해 혼자 돈 잡을 0 으로 읽게 했다 → #108 에 9e495b4 로 「자기 포함 · 혼자면 1」 을 적고 잠갔다(보고 `…-verify-s108.md`) |
| S12.3 | 통과(기록) | `gh pr view 108/106 --json mergedAt,state` → 둘 다 OPEN · `mergedAt null`. 시나리오 §3 의 순서는 #107 → #109 → **#108 → #106** → #110 이다. 다만 `docs/m5l-review-supplement-workplan.md` §3 2항은 「#107 → **#106 → #109 → #108** → #110」이라 문서끼리 어긋난다 — #106 의 절이 `step_timeline`·`concurrent_at_start` 를 말하고 그 키는 #108 이 만드니 **#108 이 먼저**여야 한다. 코디네이터 참고(이 PR 의 흠은 아니다) |
| S12.4 | 통과 | `gh pr diff 106 --name-only` → `CHANGELOG.md` · `docs/configuration.md` · `tests/test_docs_admission_two_lane.py` 셋뿐(`gate-optimization-workplan.md`·codex 리뷰 파일 없음 — rebase 됐다) |
| S12.5 | 통과 | 겹침 조건(461~465행 「while A is in its heavy phase (CPU near 100 %), lane 2 stays closed and B waits at the gate — so the win is the light phases overlapping」) · 머신 전체 락(454행 「lanes of `rcm serve` and of an `rcm worker` on the same machine share it」 + fd 상속 한 줄) · CHANGELOG `[Unreleased]` Added 11~12행 「Two lanes for a gate with a light phase and a heavy phase」 |
| S12.6 | 통과 | `pytest tests/test_docs_admission_two_lane.py -q` **8 passed** → `sh` 블록(락 문단)을 지우면 `FAILED …::test_the_experiment_needs_a_lock_in_the_script` → 되돌리고 `fcntl.flock` 줄을 `lockf -k /tmp/gate-heavy.lock true \|\| exit 3` 으로 바꾸면 **8 passed**(글자가 아니라 락의 뜻을 잠근다: `fcntl\.flock\|\blockf\b` + `\|\| exit \d` + `machine-wide lock` + 폴백 없는 `flock 9` 한 줄 금지) |

오류 0 · 이 브랜치 수정 없음 · 확인 못 함 0. (S12.2 가 드러낸 문서 어긋남은 #108 쪽 파일이라 #108 에 고쳤다.)

## 명령 그대로

```sh
# S12.1 — 이 Mac
awk '/^```sh$/{f=1;next} /^```$/{f=0} f' <(awk '/^### Two lanes for a gate/,/^## Second build/' docs/configuration.md) \
  | sed 's/^flutter test \.\.\./sleep 3/' > <data_dir>/lock.sh
which flock            # not found
( bash lock.sh; echo "first rc=$?" ) & sleep 0.3; time ( bash lock.sh; echo "second rc=$?" ); wait
sed 's#/tmp/gate-heavy.lock#/nonexistent-dir/x.lock#' lock.sh > lock-bad.sh; bash lock-bad.sh; echo rc=$?   # rc=3
# S12.2 — 8813(#108 워크트리의 서버) lanes = 2
grep -n concurrent_at_start docs/configuration.md src/remote_ci_monitor/server.py src/remote_ci_monitor/core/status.py
J6=$(rcm run sleepy … --dir j6 --no-wait | tail -1 | jq .job_id); sleep 1; J7=$(rcm run sleepy … --dir j7 --no-wait | tail -1 | jq .job_id)
rcm cancel $J6 … ; rcm cancel $J7 … ; curl -s …/jobs/$J6 | jq -c '{id,state,started_at,concurrent_at_start}' ; 같은 것 $J7
diff <(status-keys-before) <(curl -s …/api/status | jq -S keys) ; curl -s …/api/status | jq '.schema_version'
# S12.3 / S12.4
gh pr view 108 --json mergedAt,state ; gh pr view 106 --json mergedAt,state ; gh pr diff 106 --name-only
# S12.6
./.venv/bin/pytest tests/test_docs_admission_two_lane.py -q   # → sh 블록 삭제 → 다시 → 되돌림 → fcntl.flock→lockf → 다시 → 되돌림
```

실측 출력(발췌):

```
S12.1  first rc=0 / second rc=0 / ( bash lock.sh; … )  0.02s user 0.01s system 0% cpu 5.743 total
       lock-bad.sh: line 6: /nonexistent-dir/x.lock: No such file or directory / bad rc=3
S12.2  {"id":6,"state":"cancelled","started_at":"2026-09-13T02:19:31Z","concurrent_at_start":1}
       {"id":7,"state":"cancelled","started_at":"2026-09-13T02:19:32Z","concurrent_at_start":2}
       status keys unchanged · {"schema_version":1,"has_cas":false}
S12.6  8 passed → FAILED tests/test_docs_admission_two_lane.py::test_the_experiment_needs_a_lock_in_the_script → 8 passed(lockf)
```

## 비고

- S12.2 의 첫 시도는 `admission = "load"`(기본) 그대로였는데 둘째 잡(#5)이 3초 안에 시작하지 못했다(`started_at null` · `concurrent_at_start null`) — 레인 2 는 CPU 표본 `admission_samples`(3)개가 연속으로 기준 아래여야 연다. 절 461~465행이 말하는 바로 그 동작이다. 측정을 위해 `admission = "always"` 로 재기동해 #6·#7 을 동시에 태웠다.
- `fcntl.flock` 을 자식 `python3` 에서 걸어도 락은 열린 파일 기술(fd 9)에 붙으므로 python 이 끝난 뒤에도 셸이 fd 9 를 닫을 때까지 유지된다 — S12.1 의 5.7 초가 그 증거다(macOS 의 BSD flock 도 같다).
