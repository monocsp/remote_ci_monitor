# M5l 검증 보고 — L7 Chrome 붙이기의 찬 기동 마감 · 수집기 부재 잠금 · 2026-09-13 · PR #113 (`test/web-browser-attach-deadline-and-collector` @ cc80997 → 수정 a8b8815)

- 시나리오: `docs/acceptance/m5l-verification-scenarios.md` §1 L7(리뷰 `pr-98.md` B-1~B-3 · `pr-82.md` B-1 · C). 실행 주체: 격리 에이전트(verify-l7).
- 워크트리 `../remote_ci_monitor-verify-m5l-l7`(`docs/acceptance-m5l-verify-l7`, 기준 cc80997) + 자기 `.venv`. Chrome: `find_chrome()` → `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`(이 Mac). 변이는 레포 밖 스크래치 사본(`<scratch>/mut`, 자기 `.venv`)에서만 했다. 서버 없음(테스트가 자기 서버 · 8817 미사용).
- 변이 방법(L7.1~L7.3): 사본의 `Chrome.call` 에서 `deadline = time.monotonic() + timeout` 뒤, `_read_msg` 앞에 `if method == "<메서드>" and not getattr(self, "_slow_once", False): self._slow_once = True; time.sleep(N)`.

## 결과 요약

| ID | 판정 | 실측 |
|---|---|---|
| L7.1 | **오류 → 수정 a8b8815** | cc80997 사본에서 `Target.getTargets` 20 s 지연: `test_desktop_dom_shows_running_and_queued_jobs` **실패** — 단 프레임은 `_attach_first_page → call` 의 「no reply」가 아니라 `open()` 의 `page not ready within 15.0s`(본문에 잡 #2 만 running, 「waited 20s」). 원인: Chrome 은 `scene` 픽스처가 잡을 올린 **뒤에** 뜨는데 장면의 running 잡이 `test_server.PRESETS` 의 20 s `slow` 라 찬 기동 동안 끝난다 — 60 s 마감이 실제로 지키는 폭은 ~18 s 뿐. 같은 지연 17 s 는 통과(18.7 s). 수정 뒤(장면만 120 s `slow`) 20 s 지연 **통과**(21.5 s) |
| L7.2 | 오류(같은 원인) → 수정 뒤 통과 | `Target.attachToTarget` 17 s 지연 통과(19.6 s) · 수정 뒤 20 s 지연 통과(22.8 s) |
| L7.3 | 통과 | `Runtime.evaluate` 20 s 지연 — **첫** 호출은 `__init__` 의 씨앗 심기(60 s 마감)라 흡수되므로 **두 번째** 호출(`open()` 안)에 지연: 총 24.8 s 뒤 실패 `AssertionError: CDP: no reply before the deadline`, 프레임 `:317 in open → :282 in eval → :224 in call`. 붙은 뒤의 호출은 15 s 그대로 |
| L7.4 | 통과 | 사본 `__init__` 의 `Page.addScriptToEvaluateOnNewDocument` 호출 삭제 → `-k "local_host_card or remote_worker_sample"` **2 failed**, 둘 다 `AssertionError: page error collector missing`(`page_errors()` 가 `null` 을 받아 예외 — `[]` 아님) |
| L7.5 | 통과 | 사본 `app.js:1730` `h.source === "local"` → `local` : `-k local_host_card` 빨강 `page raised while loading http://127.0.0.1:…/?poll=1&lang=en: ['unhandledrejection: local is not defined']` |
| L7.6 | 통과 | `python scripts/mutcheck.py --only web-page-error-collector-removed` → `control green, mutant red (exit 1, 3.2s)` · `grep -c "Mutant(" scripts/mutcheck.py` = **27** · 전체 mutcheck 27/27 caught(수정 뒤에도) |
| L7.7 | 통과 | 주석(`:160-166`)이 「`_attach_first_page` 안의 `Target.getTargets` 는 15초 그대로라 … 세 번 더 빨갰다(ubuntu 3.11·3.13)」— 3.13 한정 없음 · `_attach_first_page` 가 `remaining() = max(1.0, deadline - time.monotonic())` 을 두 `call` 에 넘긴다(`:240-250`) |
| L7.8 | 기록(머지 전) | `gh run list --branch dev --workflow ci.yml -L 5`: 34457390683 success · 34457387029 failure(no reply **3**) · 34456635430 success · 34456628646 failure(3) · 34455895291 failure(3) — 전부 2026-09-10, #113 머지 **전**의 run. 머지 뒤 다시 센다. #113 자체의 CI(run 34720042071, a8b8815): `unit (ubuntu-latest, 3.11, google-chrome)` 6m17s pass · `(3.13, google-chrome)` 6m45s pass · macOS pass · `test` pass |
| L7.9 | 기록 | `pytest tests/test_web_browser.py -q -k find_chrome` → 수집 0건 — `RCM_CHROME` 만 보는 단위 테스트는 없다(리뷰 #82 C · P2, 이 PR 범위 밖) |

오류 1(L7.1 · L7.2 같은 원인) → 수정 **a8b8815**(`test(web): Chrome 장면의 slow 잡이 찬 기동 마감보다 오래 산다`). 확인 못 함 0.

## 수정 a8b8815

- 빨간 테스트: `test_the_scene_outlives_the_cold_start_deadline` — 장면 서버의 `slow` argv 에서 `sleep N` 을 읽어 `N ≥ COLD_START_SECONDS + 15 + 5` 를 단언. cc80997 에서 `assert 20 >= 80` 으로 빨강.
- 수정: `SCENE_PRESETS` — 장면만 `slow` 를 `SCENE_SLOW_SECONDS`(120) 초로(`timeout_seconds` 180). 공용 `test_server.PRESETS` 의 `slow` 는 그대로(`rcm wait` 가 그 완료를 기다리는 테스트 `test_cli_m1.py:265` 가 있다). 장면 픽스처는 `srv.cfg.presets = …` 로 바꾼다(모듈의 다른 두 테스트와 같은 방식). teardown 은 전처럼 취소.
- 검사: `ruff check . && ruff format --check .` 통과 · `pytest tests/test_web_browser.py` 11 passed(27 s) · 전체 `pytest -q -x` 통과(5 m 39 s) · `python scripts/mutcheck.py` 27/27 · 사본 재변이 L7.1·L7.2 20 s 통과 · #113 CI 초록(위).

## 명령 그대로

```sh
rsync -a --exclude .venv --exclude .git ../remote_ci_monitor-verify-m5l-l7/ <scratch>/mut/ && cd <scratch>/mut && python3.11 -m venv .venv && ./.venv/bin/pip install -q -e '.[dev]'
# L7.1 / L7.2 / L7.3 — 위 변이 삽입 뒤
./.venv/bin/pytest tests/test_web_browser.py -q -k test_desktop_dom_shows_running_and_queued_jobs
# L7.4
./.venv/bin/pytest tests/test_web_browser.py -q -k "local_host_card or remote_worker_sample"
# L7.5
sed -i '' 's/h.source === "local"/local/' src/remote_ci_monitor/web/app.js && ./.venv/bin/pytest tests/test_web_browser.py -q -k local_host_card
# L7.6 · L7.9 (워크트리)
./.venv/bin/python scripts/mutcheck.py --only web-page-error-collector-removed ; grep -c "Mutant(" scripts/mutcheck.py
./.venv/bin/pytest tests/test_web_browser.py -q -k find_chrome
# L7.8
gh run list --branch dev --workflow ci.yml -L 5 --json databaseId,conclusion ; gh run view <id> --log-failed | grep -c "no reply before the deadline"
```

## 비고

- L7.1 의 시나리오 기대(「20 s 지연에도 통과」)는 마감만 보면 참이었고(CDP 마감 프레임은 안 났다) 장면이 먼저 무너진 것이라, 수정은 하네스가 아니라 장면의 수명이다. CI 러너의 찬 기동이 18 s 를 넘겼다면 #113 만으로는 「no reply」 대신 「page not ready」로 여전히 빨갰을 것이다.
- L7.3 의 「약 15 s 뒤 실패」는 두 번째 `Runtime.evaluate` 에서만 성립한다 — 첫 번째는 이 PR 이 about:blank 에 수집기를 심는 씨앗 호출(60 s)이다.
